# Greystone

## Концепція і ідея

Greystone — це roguelike RPG у фентезі-сеттингу, натхненний **Cataclysm: Dark Days Ahead**, **Kenshi** і **Dwarf Fortress (Adventure Mode)**. Гравець опиняється у живому, жорстокому світі з королівствами, магією та драконами. Немає чітко прописаного сюжету — є відкритий світ, де ти сам вирішуєш що робити і ким стати.

**Головні принципи:**
- ⚖️ **Симетрія світу (фундаментальне правило):** гравець — частина світу, а не його центр. Все що може гравець — можуть NPC, і навпаки: навички, магія, бій, травми, голод, власність, спадщина, кар'єра. Будь-яка механіка пишеться для `Actor`, не для `Player`
- Світ живе без гравця — NPC працюють, торгують, воюють, старіють, одружуються, народжують дітей і помирають
- Виживання важливе: голод, спрага, травми, холод мають значення
- Все має вагу і об'єм — інвентар обмежений фізично
- Смерть — частина гри. Світ зберігається між персонажами; спадщина через заповіти. Зміна персонажів = зміна поколінь
- Прогресія без класів — усе качається через використання (зброя, магія, ремесла)
- ASCII-рендер у стилі класичних roguelike, але з сучасними зручностями (керування мишею, візуалізація шляху)

## GDD документи (дизайн зафіксовано, реалізація попереду)

| Файл | Зміст |
|---|---|
| `docs/magic.md` | 6 базових шкіл (Вогонь/Вода/Земля/Повітря/Життя/Смерть), похідні школи через комбінації (Некромантія = Життя+Смерть, Лід = Вода+Повітря...), створення власних заклять і шкіл, стаміна + час касту, невдалі касти, шляхи навчання (гільдії/книги/фракції/відлюдники) |
| `docs/weapons.md` | 11 навичок зброї (0–100), прокачка через удари, рандомні попадання по 6 частинах тіла з вагами, ефекти типів зброї (меч=кровотеча, булава=оглушення+крізь броню, сокира=sever), техніки як закляття для воїнів (вчителі, вимоги рівня), формули попадання/урону |
| `docs/village.md` | Село як живий організм: сім'ї в будинках, професії із замкнутим економічним циклом, цілі NPC = джерело квестів, вік і життєві стадії, демографія (шлюби/народження/ріст дітей/смерть від старості), спадщина, земля і власність, кар'єра гравця (ферма → староста → король) |

**Ключові рішення з GDD:**
- **Економіка двошарова (як DF):** активна зона = повна предметна симуляція (кожен предмет — реальний Item, фермер фізично сіє/жне/несе в комору, крадіжка забирає реальний хліб); віддалена зона = подієва абстракція (Legends Mode), розгортається назад у предмети коли гравець приходить
- **Час = DF Adventure Mode:** астрономічний, без штучного прискорення віку. Покоління через: worldgen з готовою історією (могили, предки), стрибки часу (сон/подорожі — демографія тикає), зміну персонажів (смерть героя = "retire", новий персонаж застає світ що жив)
- **Навички спільні для зброї і магії:** один enum `Skill`, одна структура `SkillLevel` (0–100, exp), живуть в `Actor` (симетрія). Техніки/закляття на одній панелі хоткеїв
- **Бій:** рандомні попадання (як CDDA/Kenshi, без вибору частини), шанс = 60 + Δdex×3 + навичка/3, урон = зброя × strMult × skillMult(0.5–1.3) ± 20%, броня захищає конкретну частину тіла

---

## Технічний стек

- **Мова:** C++17
- **Рендер:** SDL2 + SDL2_ttf, шрифт DejaVuSansMono 16px
- **Стиль:** ASCII символи на кольоровому фоні
- **Платформа:** Windows (mingw64)

**Команда збірки:**
```
C:/msys64/mingw64/bin/g++.exe -std=c++17 -O2 -o greystone.exe src/main.cpp src/core/astar.cpp src/core/map.cpp src/render/render.cpp -Isrc/core -Isrc/render -Isrc/entities -Isrc/ui -I"C:/msys64/mingw64/include/SDL2" -L"C:/msys64/mingw64/lib" -lmingw32 -lSDL2main -lSDL2 -lSDL2_ttf
```

---

## Архітектура

### Карта
- 200×200 тайлів, три шари: **TERRAIN** (земля) + **GROUND COVER** (покриття) + **OBJECT** (дерева, каміння)
- Кожен об'єкт має `objectHp` — міцність
- A* пошук шляху з octile heuristic і cost-based рухом

### Світ
- 100×100 секторна оверкарта (`overmap.h`) з біомами
- Детермінований генератор секторів за seed = координати
- Туман війни: відкривається при вході в сектор (3×3 навколо)
- Переходи між секторами при досягненні краю карти

### Актори (`actor.h`)
- `Actor` — базовий клас: позиція, символ, швидкість, тіло, 6 статів, голод/спрага, фракція
- `Body` — 6 частин тіла (голова, тулуб, 2 руки, 2 ноги), кровотеча, переломи, відрубування
- `Player` — 16 слотів екіпіровки, ефективні стати з предметів, інвентар у контейнерах
- `Enemy`, `Animal` — базова AI

### Інвентар (`item.h`, `inventory.h`)
- 16 слотів екіпіровки: HEAD, FACE, NECK, TORSO, OVER_TORSO, HAND_R, HAND_L, HANDS, RING_R, RING_L, BRACELET_R, BRACELET_L, WAIST, LEGS, FEET, BACK
- Предмети мають об'єм (мл) і вагу (г) — контейнер обмежений по обох параметрах
- На землі предмет показується символом категорії (`/`=зброя, `[`=броня, `%`=їжа тощо)
- Прикраси дають бонуси до STR/DEX/CON — реально застосовуються
- Валюта — фізичний предмет "Gold Coin" (`Items::goldCoin()`), використовується в торгівлі з NPC
- Стекування: `Item::count`/`maxStack` (за замовчуванням `maxStack=1` — не стекується). Стекуються валюта (Gold Coin — 100), їжа/питво (~10-20) і сировина (колоди/гілки/каміння — 20). Вільна функція `addToContainer(Item&, Item)` (item.h) — єдина точка додавання в контейнер: мерджить в існуючий стек, розбиває на кілька стеків при перевищенні ліміту, все-або-нічого (не лишає предмет "застряглим" напівдоданим)

### Взаємодія зі світом
- Правий клік на сусідній об'єкт → Chop/Mine/Harvest
- Удар = 20 + (STR-10)*2 пошкодження
- При знищенні падають ресурси: Logs, Branches, Stone, Berries

---

## Поточний стан (що реалізовано)

- [x] Карта, FOV, A* з cost-based рухом
- [x] Енергетична покрокова система (CDDA-стиль)
- [x] Ієрархія акторів з частинами тіла і кровотечею
- [x] Система рас з модифікаторами статів
- [x] Оверкарта з біомами і туманом війни
- [x] Переходи між секторами
- [x] Чіт-консоль (reveal_map, hide_map, tp X Y)
- [x] Інвентар: 16 слотів, контейнери, підбір/викидання/екіпіровка
- [x] Перегляд предметів (на землі і в інвентарі)
- [x] Шкали голоду/спраги у Body Panel
- [x] Панель характеристик з бонусами від екіпіровки і бойовими статами
- [x] Захист від броні зменшує вхідний урон
- [x] Зброя в руці додає урон
- [x] Взаємодія зі світом: рубати дерева, добувати каміння, збирати кущі
- [x] Система сіл: ~15 сіл на оверкарті (маркер `#` золотий), будівлі з стінами/підлогою/дверима, бруд-доріжки до центральної площі з криницею
- [x] Логічна генерація села: 2 ферми на випадкових сторонах світу (одна може стати Herbalist-полем), Коваль/Староста/Лісник — окремі будівлі, розташовані випадково навколо площі (Староста — ближче, з перевіркою накладання), з декором під професію (ковадло, штабелі колод). `map.cpp` вирішує роль будівлі під час генерації (`villageBuildings`), `spawnVillagers()` лише читає це
- [x] Система часу: WorldTime (хвилини/години/дні/сезони), день/ніч впливає на FOV і темряву
- [x] NPC-жителі: маршрути (WANDER/WALK_HOME/SLEEP), домівки/ліжка, привітання, Examine
- [x] Лут з ворогів: `dropEnemyLoot()` кидає предмети + лишає труп при смерті
- [x] Крафт з ресурсів: `CraftPanel`, рецепти зі списком інгредієнтів, переривання зберігає частковий предмет (resumable)
- [x] Очікування/відпочинок: `WaitPanel` (T) — прискорення часу до цілі, переривається ворогом поруч або погіршенням дебафу голоду/спраги (`ConfirmPanel`)
- [x] Панель ефектів (S): список активних бафів/дебафів (голод, спрага, кровотеча, бонуси від прикрас, світло) з поясненням
- [x] Система професій: `Occupation` (Farmer/Herbalist/Woodcutter/Blacksmith/Elder/Seamstress) — Farmer/Blacksmith/Elder/Woodcutter обов'язкові в кожному селі, Herbalist часто, Seamstress рідко
- [x] Староста говорить інакше за звичайних жителів: окремі `ELDER_GREETINGS_DAY/NIGHT` (main.cpp) замість загальних `GREETINGS_DAY/NIGHT` — авторитетний тон, а не звичайна балачка. Квести від старости — ще заплановано
- [x] Торгівля з NPC: кожен житель з професією продає своє з власної сумки (`bag`, реальний контейнер — `goodsFor()` наповнює його при спавні), `TradePanel` — купівля/продаж за фізичну валюту "Gold Coin"
- [x] Авто-повтор рубання/копання: правий клік продовжує бити ціль до знищення
- [x] `Villager` — повноцінний `Actor` (як Player/Enemy): статblock, 6-частинне `Body`, `hunger`/`thirst`/`tickNeeds()`/`takeDamage()` — усе успадковане, нічого не дублюється. Потреби: голод/спрага як у гравця, реальна їжа зі своєї сумки (`bag`, contents — жодного "невидимого" інвентаря); Farmer/Herbalist поповнюють запас збором стиглого врожаю зі свого поля, решта професій — ні (ризик голоду). Стани `EAT`/`DRINK` — йдуть додому їсти чи до криниці пити. NPC може померти від голоду/спраги (через `takeDamage()` → `Body::isDead()`), при смерті лишає труп і скидає весь вміст сумки (`dropBag()`)
- [x] `Enemy::bag` — той самий підхід: замінено "невидимий" `carried` на реальний контейнер (`Items::backpack()`), `weaponDmg()` і "with X" повідомлення читають з нього. Гобліни/орки/бандити мають раціон (їжа+вода) у сумці; скелети — ні (нежить). Голод/спрага у ворогів тепер реально вбивають (раз на перетнуту ігрову годину), а не лише кровотеча. `dropBag()` (спільний хелпер) розкидає весь вміст + порожню сумку при смерті — і в `Enemy`, і в `Villager`
- [x] Fullscreen: гра стартує в borderless fullscreen desktop під роздільність монітора; `Alt+Enter` перемикає у віконний режим і назад (`applyScreenSize()`/`toggleFullscreen()`, main.cpp)
- [x] Єдиний візуальний стиль попап-панелей (`panel_style.h`, `PanelStyle::frame()`/`dimBackdrop()`) — Trade/Examine/Craft-confirm/Pickup/Wait/Confirm використовують спільну рамку/фон/заголовок замість кожен свого
- [x] Меню-хаб (`menu_hub.h`, `MenuHub`) — Character/Body/Effects/Skills/Inventory/Craft/Map об'єднані в одне повноекранне вікно зі смугою вкладок зверху; перемикання гарячими клавішами (O/B/S/K/I/C/M — працює навіть з середини іншої вкладки) або кліком по вкладці, кнопка закриття (X) в кутку
- [x] Бойова система жителів: атака (лівий клік/контекстне меню — нейтральних тільки впритул, `isHostile()`-гейт від випадкових вбивств), реакція `FIGHT`/`FLEE` за професією + наявністю зброї в сумці (`rollCombatReaction()`), свідки в радіусі бачення теж реагують (`villagerReactToAttack()`), боєць із низьким HP ламається і тікає — той самий патерн, що `Enemy::wantsToFlee()`
- [x] "Підійти, потім щось зробити" — атака й розмова з жителем здалеку спершу підводять гравця впритул (chase-to-attack патерн з Enemy, поширений на `attackTargetVillager`/`talkTargetVillager`, шлях перераховується щокроку якщо ціль рухається)
- [x] Одяг жителів (`Villager::outfit`, окремо від `bag`) — завжди одягнені, одяг падає при смерті разом з рештою лута
- [x] Трупи й предмети на землі прив'язані до сектора (`Corpse`/`GroundItem::sectorX/sectorY`) — більше не зникають при виході з сектора; фільтруються за поточним сектором при рендері/взаємодії (координати 0-199 повторюються в кожному секторі)
- [x] Робочий діалоговий режим (`PanelMode::DIALOGUE`, `BottomPanel`) — "Talk" показує репліку жителя й список відповідей (Trade/Ask about their work/Farewell) у нижній панелі замість одного рядка в лог. Не повне дерево — один рівень глибини, "Ask" перебудовує той самий список
- [x] Фундамент бойової системи (`skill.h`, `combat.h`) — `Skill` enum (зброя+магія) + `SkillLevel` (0–100, exp, прокачка промах+1/влучання+2/вбивство+5) живуть у **Actor** (не Player) — кожен ворог і житель має власні навички. `weaponSkill`/`sharp` на `Item`, проставлено на всій реальній зброї. `combat.h::resolveAttack()` — єдина формула попадання/урону (шанс = 60+Δdex×3+навичка/3, множник урону 0.5–1.3 від навички, ±20% розкид, head×1.5) замінила 8 копіпастних блоків у `main.cpp`; тепер існують промахи (їх не було). Ворожі фабрики (`enemy_types.h::trainSkill()`) дають стартовий рівень під зброю яку носять — Goblin Dagger 15-20, Skeleton Mace 20-28, Wolf Unarmed 15-25, Orc/Bandit 25-35 (кожен рандом у діапазоні); жителі лишаються 0 (не бійці). Ефекти типів зброї (кровотеча/оглушення/sever), техніки і магія — ще не зроблені
- [x] Панель навичок (`skills_panel.h`, `SkillsPanel`, хоткей K) — вкладка в MenuHub, показує всі 17 `Skill` гравця (зброя ліворуч, магічні школи праворуч) з рівнем і прогрес-баром до наступного. Магічні школи показують рівень 0, поки немає кастування — очікувано
- [x] Ефекти типів зброї (частково, `combat.h`/`actor.h`) — **меч**: гарантована кровотеча на будь-якому влучанні (не тільки при важкій рані), **сокира**: підвищений шанс sever при добиванні (65% замість базових 40%), **булава**: ігнорує 50% захисту захисника, **кинджал**: ×1.5 урону по цілі що не помітила атакуючого (наразі підключено лише для сплячого жителя — `Villager::State::SLEEP`). Свідомо не зроблено: оглушення булавою (пропуск ходу — немає механіки stun/turn-skip), швидша дія кинджалом (усі дії коштують фіксовані 100 енергії), спис на 2 тайли, ефекти кулаків/щита
- [x] Лікування кровотечі (`Item::curesBleed`, `herbalPoultice`) — гарантована кровотеча мечем була б нечесною без ліків (`bleed` ніде природно не спадав), тому "Use" на Herbal Poultice тепер реально скидає кровотечу з усіх частин тіла гравця, а не просто мала опис-обіцянку. Поки що тільки гравець — NPC не лікують себе самі (симетрія тут не повна, свідомо відкладено)
- [x] Стаміна (`Actor::stamina`/`maxStamina`) — `maxStamina = 50 + constitution×5`, регенерує +3/хід через `tickNeeds()` (той самий виклик що й у голоду/спраги, тож і ворог, і житель регенерують так само). `hasStamina()`/`spendStamina()` готові як інтерфейс — нічого ще не витрачає стаміну, це прийде з першою технікою/закляттям. Показ у HUD (`bottom_panel.h`, STA: поруч з NRG)
- [x] Перші техніки (`combat.h::Technique`/`TechniqueId`) — **Brutal Strike** (Сокира 20, гарантована глибока рана+кровотеча), **Lunge** (Меч 30, +25 до шансу влучання), **Backstab** (Кинджал 40, ×2 урону, лише проти сплячого жителя — для ворогів немає системи "чи помітив"). Коштують стаміну + додатковий час понад звичайний удар (`onPlayerAct(extraEnergy)`). З'являються в правоклік-меню атаки лише коли рівень навички під поточну зброю в руці досягнутий (без окремого "вивчено від вчителя" — це наступний крок). Атака технікою вимагає вже стояти впритул — авто-підходу як у звичайної "Attack" ще нема
- [x] Чіт-команди для тестування навичок/технік (`cheat_console.h`) — `setskill <skill> <0-100>` виставляє будь-яку з 17 навичок напряму (`SKILL_TOKENS`, порядок мусить збігатись з enum `Skill`), `unlocktech <name>` піднімає навичку до мінімального рівня потрібної техніки (`TECH_TOKENS`, порядок = enum `TechniqueId`) — не окремий "unlocked"-прапорець, той самий гейт що й у грі
- [x] Панель технік + хотбар (`techniques_panel.h`, `hotbar.h`, хоткей Y) — вкладка Techniques в MenuHub показує всі техніки (розблоковані/заблоковані, вимога, вартість стаміни/часу) і 9 квадратиків для ручного призначення на слот (клік — призначити, повторний клік по тому самому слоту — зняти). Хотбар з 9 слотів завжди на екрані під час геймплею (ховається лише коли відкритий сам хаб), поверх нижньої частини вьюпорту карти. Клавіші 1-9 і клік мишкою по квадратику викликають техніку зі слоту — авто-таргетить першого живого ворога впритул, потім першого жителя впритул (той самий вибір цілі що й "Attack" в контекстному меню). Порожній слот / незасвоєна техніка / брак стаміни — повідомлення в лог, дію не витрачено
- [x] Хотбар-прицілювання узагальнено на техніки й закляття (`techniqueTargeting` → `hotbarTargeting`, `main.cpp`) — один режим прицілювання на обидва типи дій, замість дублювання
- [x] Чотири закляття школи Вогню (`magic.h`, вкладка Magic/хоткей P) — **Spark** (Fire 0, `SpellShape::POINT`, слабкий снаряд), **Fireball** (Fire 15, `SpellShape::BURST` радіус 1 — вибухає навіть якщо не влучив точно в ціль), **Wall of Fire** (Fire 20, окрема multi-tile-select система — див. окремий пункт нижче), **Explosion** (Fire 35, `SpellShape::BURST` радіус 2). Кожен спел б'є **тайл, не актора** — клік ніколи не скасовується через "нема цілі" (`useSpellAtTile()`, `main.cpp`): POINT чіпає лише сам клікнутий тайл, BURST будує footprint по радіусу й чіпає кожного ворога/жителя в ньому; на порожньому тайлі Fire-заклинання кидає кубик спершу підпалити флаговану сцену (`tryIgniteObject()`, 30% — дерева/сухостій/кущі/пшениця/пень, реально б'є по `objectHp` і при знищенні валить об'єкт через `destroyWorldObject()`, той самий лут-шлях що й рубання/копання), а якщо об'єкта нема — траву/мох/листя (`tryIgniteGround()`, 30%, `GroundId::G_SCORCHED`, суто косметичний свап ground cover — без розповсюдження/DoT для жодного з двох). `destroyWorldObject()` — витягнутий з `interactWithObject()` спільний шматок (свап тайла + лут + повідомлення), тепер один код на знищення об'єкта для сокири/кайла і для вогню. `SpellShape::BURST`-заклинання (Fireball/Explosion) також малюють тьмяний спалах по всьому радіусу вибуху (`SpellBurst`/`renderSpellBurst()`, `main.cpp`) — з'являється з затримкою що збігається з польотом снаряда (`SpellProjectile::duration`), потім гасне; суто косметика, урон уже пораховано миттєво при каст-резолві. Бекфайр і приріст навички рахуються раз на каст (`rollSpellCast()`), окремо від per-target урону (`resolveSpellHit()`), щоб Explosion по натовпу не кидав кубик невдачі і не качав скіл кілька разів. Каст = стаміна + додатковий час (`onPlayerAct(extraEnergy)`), націлювання — та сама система що й техніки (`hotbarTargeting`), тільки без вимоги "стояти на акторі" — техніки (мілі) досі вимагають ціль під курсором, спели ні. Слоти хотбара кодують і техніку, і закляття в тому самому `int[9]` через offset (`Hotbar::SPELL_SLOT_OFFSET`). `unlockspell <name>` у чіт-консолі за зразком `unlocktech`. Свідомо не зроблено: Тепло (немає системи температури/холоду взагалі), Вогняний щит (реактивний тригер на вхідний удар — немає хука), Метеор (затримка/попередження перед вибухом — немає механізму відкладеної дії), Імунітет до вогню (немає типізації урону — `takeDamage()` бере просто `int`), пасивні заклинання (жодного поки нема), інші школи
- [x] Wall of Fire — перший справжній tile-hazard у грі (`magic.h`/`main.cpp`) — гравець замість одного кліку **сам вибирає кілька тайлів** у радіусі (`Spell::manualArea`, окремий режим прицілювання `wallTargeting`/`wallTargetSlot`/`wallSelection` — мутуально ексклюзивний з `hotbarTargeting`): лівий клік додає/знімає тайл (`toggleWallTile()`, той самий toggle що й `Hotbar::assign()`), Enter підтверджує каст (`confirmWallTargeting()`), правий клік/Esc скасовує без витрат. Вартість — **за тайл** (`Spell::staminaCost` тепер per-tile для `manualArea`, повідомлення після кожного кліку показує поточну суму), тому більше тайлів = дорожче, як просив користувач. При підтвердженні: один бекфайр-ролл на весь каст (`rollSpellCast()`), потім кожен позначений тайл або б'є актора що там стоїть (`resolveSpellHit()`, включно з гравцем якщо позначив свій тайл), або шкодить об'єкту/траві (`applySpellObjectDamage()`/`tryIgniteGround()`), і в будь-якому разі отримує `FireHazardTile` (новий глобальний `fireHazards`, `igniteFireHazardTile()` — рефреш таймера якщо тайл вже горів, а не дублікат) на `hazardTurns` ходів (Wall of Fire: 4). `tickFireHazards()` — раз на дію гравця (`onPlayerAct()`, той самий каданс що й `tickEnemyNeeds()`), палить `baseDamage` кожному хто стоїть на активному тайлі (гравець/ворог/житель — симетрично), і зменшує лічильник, знімаючи тайл коли він добіг до нуля. Рендер: `renderFireHazards()` — мерехтливий заливка (альтернуючий alpha через `SDL_GetTicks()`) під акторами, малюється одразу після `renderMap()` як шар землі; `renderWallTargeting()` — прев'ю під час вибору (тьмяний тон = у радіусі, яскравий колір заклинання = вже позначено). Свідомо не зроблено: вогонь не розповсюджується на сусідні тайли сам, немає "хто підпалив" атрибуції для репутації/свідків (крім `villagerReactToAttack()` при прямому влученні)
- [x] Магія реально шкодить об'єктам і власному касту — **`Material`** (`terrain.h`: NONE/WOOD/STONE/METAL, нове поле `ObjectDef::material`, проставлено всім 21 об'єктам) + **`materialResistance(Skill, Material)`** (`main.cpp`) визначає скільки урону школа реально завдає матеріалу — зараз тільки Fire: дерево ×1.6, камінь ×0.15, метал ×0.4, все інше (і будь-яка майбутня школа) ×1.0 за замовчуванням. **`applySpellObjectDamage()`** замінив старий `tryIgniteObject()` — тепер це детермінований урон (не шанс 30%) по **будь-якому** об'єкту в footprint будь-якого заклинання (не тільки Fire — "люба магія" per user), не тільки "горючому" списку: стіна від фаєрбола реально втрачає HP і при знищенні валиться каменем (`destroyWorldObject()`, новий кейс `O_WALL` — раніше стіна взагалі не давала лута навіть від кайла, це заразом і фікс). Об'єкти з `durability<=0` (криниця) — недоторканний сентінел, постріл поглинається без ефекту, як і при звичайній взаємодії. **Гравець тепер теж ловить власний вибух** — `useSpellAtTile()`/`confirmWallTargeting()` перевіряють чи `(tx,ty)` з footprint збігається з позицією гравця, і якщо так — `resolveSpellHit(player, player, id)` б'є самого себе тим самим розрахунком, що й ворога (раніше кинути фаєрбол собі під ноги було безкарно). Свідомо не зроблено: material-таблиці для Water/Earth/Air/Life/Death (їх ще нема), явна протидія типу "лід проти вогню"
- [x] Перша школа після Вогню — Вода (`magic.h`/`main.cpp`) — **Create Water** (Water 0, новий тип закляття `Spell::selfCast` — миттєво п'є `player.thirst` без жодного прицілювання/кліку, `useSelfSpell()`) і **Water Jet** (Water 0, `SpellShape::POINT`, той самий патерн що Spark). Перше `selfCast`-закляття в грі: `useHotbarSlot()` розпізнає `s.selfCast` і одразу резолвить на самому касторі, минаючи `hotbarTargeting`/`wallTargeting` цілком — стамина й бекфайр-ролл (`rollSpellCast()`) все одно рахуються, тільки нема "точки удару". Water-заклинання гасять активні тайли Wall of Fire (`tryExtinguishFire()`, викликається для кожного тайла footprint незалежно від того що там ще є — можна одночасно й погасити вогонь, і вдарити ворога що в ньому стоїть) — перша міжшкільна взаємодія в грі. Заразом узагальнив формулювання бекфайру ("fizzles and burns" → "backfires, hurting") — старий текст був специфічно про вогонь, для Water не пасував. Свідомо не зроблено: Учень/Адепт/Майстер рівні Води (Уповільнення, Дощ, Заморозка — Заморозка додатково вимагає Повітря)
- [x] Заклять-таблиці розбито по школах (`spells_fire.h`/`spells_water.h`/`spells_earth.h`/`spells_air.h`) — на прохання користувача, щоб список заклять не звалювався в один суцільний масив в `magic.h` в міру росту шкіл. Кожен файл — фрагмент (`inline constexpr Spell NAME_SPELL = {...}`), підключається лише з `magic.h` одразу після оголошення `Spell`/`SpellShape` (щоб уникнути циклічного інклюда — фрагменти НЕ інклудять `magic.h` самі), `spellInfo()` збирає їх у фінальний масив за `SpellId`. Уся логіка кастування (`useSpellAtTile`, `confirmWallTargeting`, `tickFireHazards`, `materialResistance` і т.д.) свідомо лишилась у `main.cpp` — окремий винос обговорювався, але відкладено (вищий ризик через extern-залежності від `player`/`enemies`/`villagers`/`panel`)
- [x] Перша школа Землі — Stone (`spells_earth.h`) — Earth 0, `SpellShape::POINT`, той самий патерн що Spark/Water Jet. Skin Hardening (другий Novice-спел Землі, тимчасовий бонус захисту) свідомо відкладено — потребує buff-duration системи, якої в грі ще нема (та сама, що відклали для Fire Shield)
- [x] Перша школа Повітря — Gust (`spells_air.h`) — Air 0, `SpellShape::POINT`, найслабший урон з усіх заклять (докс: "найслабший урон але найкращий контроль"), плюс новий ефект `Spell::knockback` — після удару відштовхує ціль на 1 тайл у напрямку від каста (`tryKnockback()`, `main.cpp`, generic над `Actor&` — працює для ворога/жителя/гравця симетрично), мовчки не спрацьовує якщо клітинка позаду зайнята/непрохідна/за межами карти. Заразом виправив баг: повідомлення про смерть від закляття завжди казало "dies, charred" незалежно від школи (лишилось з часів коли існував лише Вогонь) — тепер `", charred"` тільки для Fire, інші школи просто "dies." (стосується `useSpellAtTile()`; `confirmWallTargeting()` не чіпав — Wall of Fire завжди вогонь, там "charred" і далі коректний). Легкість (другий Novice-спел Повітря, тимчасовий бонус швидкості) свідомо відкладено — та сама buff-duration система
- [x] Панель заклять згрупована по школах (`spells_panel.h`) — на прохання користувача: зліва сайдбар шкіл (той самий left-list/right-content патерн що в `InventoryPanel`), клік перемикає `selectedSchool`, справа тільки закляття цієї школи. Список шкіл динамічний (`availableSchools()` — збирає унікальні `Spell::school` з `spellInfo()`, а не хардкоджений масив), тож нова школа з'являється в сайдбарі сама, щойно в неї є хоч одне закляття
- [x] По одному новому закляттю для кожної існуючої школи (окрім Вогню — там усе, що не вимагає нової системи, вже зроблено) — обирав те, що лягає на існуючу архітектуру без нової інфраструктури:
  - **Rain Call** (Water 15, `spells_water.h`) — новий тип `Spell::rainCall`: чистий utility-BURST, не б'є нікого, гасить усі активні тайли `fireHazards` в радіусі й змиває `G_SCORCHED` назад у `G_GRASS` (той самий `tryExtinguishFire()` з Water Jet, просто по площі)
  - **Stone Wall** (Earth 15, `spells_earth.h`) — новий тип `Spell::buildsWall`: єдине закляття що редагує тайл мапи напряму (докс: "єдина школа що редагує тайли карти") — ставить `O_WALL` на чистій прохідній клітинці в радіусі; провалюється (з витратою касту) якщо клітинка зайнята актором/об'єктом
  - **Vortex** (Air 30, `spells_air.h`) — нуль нового коду: просто `SpellShape::BURST` + вже існуючий `Spell::knockback` разом — відкидає всіх у радіусі, той самий шлях що й Gust
  
  Обидва нові типи (`rainCall`/`buildsWall`) резолвляться окремими гілками на початку `useSpellAtTile()`, до звичайної footprint/hit-логіки — повністю обходять `resolveSpellHit()`
- [x] Візуальна відмінність Gust/Vortex + анімація відкидання — Gust (`>`, крижано-блакитний) і Vortex (`@`, пилюжно-жовтий) мали майже однаковий символ/колір, важко було зрозуміти що спрацювало (user feedback). Заразом додав сам ефект knockback — раніше ціль просто мовчки телепортувалась на сусідній тайл. **`KnockbackFlash`** (`main.cpp`, вектор — не один слот як `SpellBurst`, бо Vortex може відкинути кількох одразу) — гліф самої цілі (`Actor::symbol`/`color`) ковзає зі старої клітинки на нову за ~150мс, `tryKnockback()` сам викликає `spawnKnockbackFlash()` перед переміщенням, `renderKnockbackFlashes()` малює й вичищає протухлі
- [x] Три нові закляття Землі поверх Stone Wall (усі в `spells_earth.h`), на прохання користувача. Дальність (`range`) поодиноких заклять зменшено на прохання користувача: **Stone Wall** і **Reclaim Wall** тепер `range=2` (було 5 — впритул/на кілька кроків, не через пів карти). **Architect** лишився `range=6` (пробували 3, повернули назад) — багатотайловому будівництву потрібен ширший захват. Wall Throw теж `range=6` для самого кидка (тільки радіус вибору стіни зменшено, дивись нижче):
  - **Reclaim Wall** (Earth 20) — новий тип `Spell::reclaimsWall`: прибирає стіну на клікнутій клітинці, але **тільки якщо там `O_WALL` конкретно** (не просто `Material::STONE` — перевірка по `ObjectId`), щоб закляття Землі не чіпало дерев'яні об'єкти які випадково поділяють матеріал. Без лута — стіна "тоне назад у землю", не розсипається каменем
  - **Wall Throw** (Earth 25) — новий тип `Spell::throwsWall`: б'є ціль важким уроном (18 базового — найбільше з POINT-заклять) + `knockback=true` (стіна що летить у тебе має відкидати). Спочатку робив auto-pick найближчої стіни до гравця, але користувач попросив вибір вручну — тепер **двофазне прицілювання** (`wallThrowTargeting`, окремий режим від `hotbarTargeting`/`wallTargeting` — жоден із двох не підходив, потрібні рівно два кліки з різними правилами валідності): перший клік вибирає яку `O_WALL` кинути (радіус вибору `WALL_THROW_PICK_RADIUS = 3` навколо гравця — "піднімаєш зблизька", менший за `s.range = 6`, дальність прицілу по цілі — "кидаєш дальше"), другий — куди кинути (звичайний `s.range`). Невалідний клік на будь-якій фазі скасовує все, а не перепитує. `castWallThrow()` — сам ефект (той самий що раніше сидів у `useSpellAtTile()`, просто тепер викликається з `resolveWallThrowClick()` після другого кліка); `renderWallThrowTargeting()` — прев'ю: фаза 1 підсвічує стіни в радіусі пошуку червоним, фаза 2 — звичну ціль-підсвітку
  - **Stone Wall тепер без "кидка"**: прибрав `spawnSpellProjectile` для `buildsWall`/`reclaimsWall`/`throwsWall` (перші два — не мають літючого об'єкта взагалі, `throwsWall` спавнить свій власний з позиції стіни) — замість польоту тепер миттєвий `spawnSpellBurst(tx,ty,0,color,0)` — спалах радіусом 0 без затримки прямо на цілі, виглядає як "стіна сама піднялась", а не "я щось кинув" (user feedback)
  - **Architect** (Earth 50, Master, `Spell::manualBuild`, радіус вибору тайлів 6) — та сама multi-tile-select система що й Wall of Fire (`wallTargeting`/`confirmWallTargeting()`, `useHotbarSlot()` тепер пускає в цей режим і `manualArea`, і `manualBuild`), тільки замість `FireHazardTile` кожна чиста позначена клітинка отримує `O_WALL`. Вартість — за тайл, як і Wall of Fire
- [x] Buff-система (нарешті) — тричі відкладали (Fire Shield, Skin Hardening, Lightness чекали на неї ще з сесій Fire/Earth/Air). Прості лічильники ходів прямо на `Actor` (`actor.h`) — не generic buff-list, бо їх мало, довідникова структура була б складнішою за те що вона замінює. Тикають в `tickNeeds()` (той самий каданс що голод/спрага/стаміна-реген — раз на дію актора, працює однаково для гравця/ворога/жителя за принципом симетрії). `Spell::buffTurns` — скільки ходів триває ефект; для self-cast бафів `useSelfSpell()` вирішує який лічильник виставити за `SpellId` (той самий підхід що й `CREATE_WATER`), для Slowness (не self-cast — цільовий дебаф) новий `Spell::slows` виставляє його на **цілі** прямо в бойовому циклі `useSpellAtTile()`, поруч з `knockback`:
  - **Fire Shield** (Fire 30, `spells_fire.h`) — поки активний, відбиває третину вхідного мілі-урону назад нападнику. Хук прямо в `combat.h::resolveAttack()` (новий `AttackResult::reflectedDamage`) — працює симетрично для будь-якої пари атакер/захисник, хоч зараз реально спрацьовує лише коли ворог/житель б'є гравця (бо тільки гравець кастує заклинання). `enemyAct()`/`villagerCombatAct()` (`main.cpp`) показують повідомлення й обробляють смерть нападника від відбитого урону (`dropEnemyLoot`/`dropVillagerLoot`, якщо відбилося смертельно)
  - **Skin Hardening** (Earth 0, `spells_earth.h`) — +8 захисту, хук у `Player::totalDefense()`. Поки тільки гравець — у ворогів/жителів нема еквівалентного розрахунку захисту щоб зачепитись
  - **Lightness** (Air 0, `spells_air.h`) — +15 ефективної швидкості, хук у `tickWorld()` для гравця й ворогів (у жителів окрема система руху, не energy/speed — хук поки не для них)
  - **Slowness** (Water 15, `spells_water.h`) — цільовий дебаф (не self-cast): POINT-удар слабким уроном + `-15` ефективної швидкості цілі (`slowedTicks`, docs: "Мокрий ворог повільніший"). Той самий `tickWorld()`-хук що й Lightness, тільки віднімає замість додає
  - **Acceleration** (Air 30, `spells_air.h`) — сильніший і коротший варіант Lightness (`+30`/15 ходів проти `+15`/25), окремий лічильник `accelTicks` бо величина бонусу різна. Докс описує це як "додатковий хід" (буквально зайва дія за той самий такт) — це вимагало б перебудови самого turn-loop, тому свідомо спрощено до сильнішого спід-бафа замість чесно реалізованого зайвого ходу
  - Усі п'ять відображаються в Панелі ефектів (S) з лічильником ходів що лишились (`effects_panel.h`)
- [x] Школи Життя і Смерть — останні дві базові школи, обидві з одним Novice-заклинанням (той самий "почати з малого" патерн що й Water/Earth/Air), кожна у своєму `spells_<school>.h`:
  - **Minor Heal** (Life 0, `spells_life.h`) — self-cast, лікує 15 HP торсу гравця (той самий "завжди торс" підхід що вже використовують голод/спрага/кровотеча для недирекційного HP-урону — тепер і для HP-відновлення). Перше лікування в грі взагалі — раніше навіть Herbal Poultice лікував тільки кровотечу, не HP напряму
  - **Withering** (Death 0, `spells_death.h`) — звичайний POINT-урон, той самий патерн що Spark/Water Jet/Stone/Gust. Докс описують це закляття як таке що ігнорує броню — насправді це вже неявно правда для **будь-якого** закляття (`resolveSpellHit()` ніколи не віднімає захист цілі, на відміну від `resolveAttack()` для мілі), тому це не є реальною відмінністю поки що
  - Sense Life/Sense Death (другі Novice-закляття обох шкіл — бачити живих/мертвих крізь стіни) свідомо відкладено — потребують reveal-механіки, якої в грі ще нема
- [x] Розширені символи (CP437-стиль) — рендер перемкнуто з `TTF_RenderText_Solid` (Latin-1) на `TTF_RenderUTF8_Solid` у всіх ~25 файлах, що малюють текст, тож звичайний DejaVu Sans Mono (без заміни шрифту) тепер коректно показує символи поза ASCII. **Wall** — `█` (FULL BLOCK, було `#`, `terrain.h`) — підтверджено, виглядає добре: Block Elements (U+2580-259F) шрифт має. **Villager/Bandit** — пробував `☺` (WHITE SMILING FACE, U+263A), але DejaVu Sans Mono, вочевидь, НЕ має гліфа з блоку Miscellaneous Symbols (♥♦♣♠☺☻ і т.д.) — рендерилось криво, повернуто назад на `"@"` для обох. `renderVillagers()` тепер реально читає `v.symbol` замість хардкодженого `"@"` (був баг — лишив цей фікс, він не залежить від того, який символ у `v.symbol`). Символи вкладаються прямо як UTF-8 літерали в C-рядки (`const char* symbol`) — mingw g++ пропускає їх байт-в-байт. Заплановано (не зроблено): або підібрати "людський" символ з Geometric Shapes/Box Drawing (блоки, які шрифт точно підтримує), або поставити окремий CP437 bitmap-шрифт для повного автентичного вигляду з ♥♦♣♠☺
- [x] Справжня сім'я замість геометричного гурту ліжок (`npc.h`, `main.cpp::spawnVillagers()`) — до цього "домогосподарство" було просто ліжками в радіусі 8 тайлів зі спільним прізвищем, без жодних реальних зв'язків і з повністю незалежним віком кожного жителя. Тепер `Villager` має `spouseId`/`motherId`/`fatherId`/`childIds` (індекси у `villagers`, стабільні на весь час життя села — жителі ніколи не видаляються з вектора, лише позначаються `!alive`, і весь вектор перебудовується разом при зміні сектора):
  - **Подружжя**: будь-яке дволіжкове домогосподарство (ферма/травниця/староста) лінкується `spouseId` в обидва боки; другий вік більше не незалежний рандом — перекидається близько до першого (`generateAge(HUMAN, age±10)`) замість двох випадкових дорослих 18-80
  - **Діти**: лише ферма/травниця (одружена пара), 60% шанс на 1-3 дітей, вік молодший за молодшого з батьків мінімум на 16 років (`maxChildAge = min(parent ages) - 16`, пропускається якщо не влазить). Дитині нема окремого ліжка (`map.cpp` ставить рівно 2 ліжка на будинок — свідома архітектурна межа), тому вона ділить домашній тайл з батьком, шукаючи вільну сусідню прохідну клітинку в радіусі 2 щоб не стакатись з батьками/іншими дітьми. `occupation = NONE`, власний маленький запас їжі (3 хлібини) — та сама спрощена модель "нема комори", кожен житель носить свою їжу
  - `VillagerExaminePanel` показує вік і рядки Spouse/Parents/Children (імена резолвляться в `main.cpp` в момент побудови контекстного меню "Examine", бо панель зберігає лише копію `Villager`, а не посилання на живий вектор)
  - Свідомо не зроблено (наступний крок з `village.md`): брати/сестри між домогосподарствами, вдівці/повторний шлюб, спадщина, "сліди історії" (могили — нема навіть типу об'єкта), і сама демографія — ніхто не старіє, не одружується і не народжується під час гри, все лише worldgen-знімок при заселенні села
- [x] Комора (`npc.h`, `main.cpp::spawnVillagers()`) — реальний спільний контейнер їжі на сім'ю замість того щоб кожен житель тягнув усе в особистій сумці. Фізично це вже існуюча декоративна бочка (`O_BARREL`), яку `map.cpp` й так ставить у кожній фермі/травниці — `findNearbyBarrel()` шукає найближчу до ліжка господаря (радіус 12, надійно влучає у "свою" бочку, а не в бочку коваля/лісника — ті стоять в геть іншій будівлі far away). Тільки фермерські/травницькі домогосподарства мають комору; власне `Item` (новий `Items::grainBarrel()`, велика ємність, без слота екіпіровки — не носиться) лежить лише на `Villager` головного робітника, а `granaryOwnerId` (виставляється і господарю, і подружжю, і кожній дитині) — спільний покажчик на неї, той самий патерн що й `spouseId`/`childIds`. Пріоритет їжі при `State::EAT` тепер триступеневий (`eatFrom()` — спільна лямбда): 1) власна сумка, 2) сімейна комора через `granaryOwnerId`, 3) лише для фермера/травниці — збір стиглого врожаю. Заразом виправив приховану спрощеність: збір врожаю раніше просто скидав голод у 0 без жодного `Item` (суперечило принципу "жодного невидимого інвентаря"); тепер урожай реально кладе 3 порції їжі (`Items::flatbread()`/`Items::mushroomStew()`, залежно від професії — ті самі предмети що продає торговець) в комору через `addToContainer()`, і одну з них одразу з'їдає — 2 лишаються сім'ї. Стартовий запас комори — 8 порцій при спавні. Свідомо не зроблено: гравець не може відкрити/оглянути бочку-комору як контейнер (декоративні бочки коваля/лісника й досі неінтерактивні, тож це узгоджено з рештою гри, а не half-finished) — це наступний крок якщо знадобиться; сама "економіка предметами" (сіє→жне фізично, торгівля між NPC) — окремий пункт нижче, досі не зроблено

## Заплановано

### Найближчий фокус: село до ідеалу (roadmap з village.md)

- [ ] **Worldgen сімей**: ~~будинки → сім'ї~~ базові зв'язки (подружжя/батьки/діти) зроблено вище; лишилось — брати/сестри між домогосподарствами, вдівці, сліди історії (могили, спадок)
- [ ] **Комора**: ~~контейнер-об'єкт у будинку, спільний на сім'ю~~ зроблено вище; лишилось — гравець не може її відкрити як контейнер, і сама фізична передача (фермер несе врожай, а не він телепортується в комору) досі спрощена
- [ ] **Економічні цикли предметами**: фермер сіє → жне → носить у комору; передача Item NPC↔NPC (торгівля між жителями — коваль купує хліб у фермера)
- [ ] **Вік і стадії**: дитина/підліток/юнак/дорослий/літній/старий — вплив на speed/strength (навички НЕ падають — старі майстри = вчителі), смерть від старості біля maxAge раси
- [ ] **Демографія**: шлюби (в т.ч. між селами), народження, ріст дітей, учнівство (син переймає професію або йде до майстра), нові будинки — село фізично росте
- [ ] **Спадщина NPC**: майно/земля → дітям; без спадкоємців → старості на продаж (ті самі правила що для гравця)
- [ ] **Цілі NPC → квести**: 1–2 довгострокові цілі на жителя, квест = прохання допомогти з реальною ціллю (мисливець сам полює на того вовка)
- [ ] **Земля**: `owner` на ділянках, купівля/продаж у старости
- [ ] **Шлях до старости**: репутація + багатство + підтримка жителів
- [ ] Подієва симуляція віддалених сіл (Legends-шар) + розгортання назад у предмети

### Системи персонажа (roadmap з weapons.md / magic.md)

- [ ] **Оглушення булавою**: потрібна механіка "пропуск ходу" (stun/turn-skip) — не існує для жодного actor'а, включно з гравцем; ігнор 50% броні вже зроблено
- [ ] **Швидкість дії від зброї**: кинджал має бути дешевшим по часу — зараз кожна дія (рух/удар) коштує фіксовані 100 енергії, немає per-weapon variance
- [ ] **Спис**: атака за 2 тайли (тримає дистанцію) — потребує зміни range-перевірки атаки
- [ ] Ефекти кулаків (якають силу) і щита (блок+поштовх) — ще не має механіки блоку взагалі
- [ ] **Техніки — розширення**: вчитель-ветеран у селі (зараз техніки відкриваються автоматично за рівнем навички, без окремого "вивчено від когось"), Обезголовлення/Оглушливий удар/Поштовх щитом з таблиці weapons.md, chase-to-technique (зараз тільки впритул), Backstab проти ворогів (потребує системи "чи ворог мене помітив" — є лише для сплячого жителя)
- [ ] **Магія — решта школи Вогню й інші школи**: Тепло (потребує систему температури/холоду — її взагалі нема), Вогняний щит (реактивний тригер на вхідний удар), Метеор (відкладена/телеграфована дія), імунітет до вогню (потребує типізації урону — `takeDamage()` не розрізняє physical/fire), потім Вода/Земля/Повітря/Життя/Смерть. Каст зараз атомарний (як техніки) — багатоходовий переривний каст ("рух скасовує каст" з `magic.md`) ще не зроблено, поки що не критично при castTime 1-2 ходи

### Інше

- [ ] Система фракцій (`factionId` на Actor і досі не використовується; `disposition` тепер реально керує бойовою поведінкою жителів)
- [ ] Збереження / завантаження
- [ ] Повноцінні діалоги з гілками — базовий режим є (привітання + Trade/Ask/Farewell), справжнього дерева з авторським контентом ще нема
- [ ] Фаст-тревел по оверкарті
- [ ] Худоба/тваринництво (Rancher), м'ясник, винороб, тесляр — потребують системи тварин/полів за типом, поки що немає
- [ ] Ремонт предметів у коваля (зараз коваль лише продає)
- [ ] Торгівля між NPC (щоб не-фермери могли поповнювати їжу) — частина економічних циклів вище

---

## Файли

Код лежить у `src/`, розбитий на 4 підпапки за роллю. Заголовки лишаються
з пласкими `#include "file.h"` — компілятор шукає їх через `-I` на кожну
підпапку (див. команду збірки вище), а не через відносний шлях.

Дизайн-документи лежать у `docs/`: `magic.md`, `weapons.md`, `village.md`, `world.md`.

| Файл | Призначення |
|---|---|
| `src/main.cpp` | Головний цикл, input, AI, переходи секторів |
| `src/core/astar.cpp/h` | A* пошук шляху, константи карти |
| `src/core/map.cpp/h` | Tile, generateSector, updateVisibility |
| `src/core/terrain.h` | TerrainDef / GroundDef / ObjectDef + BiomeType |
| `src/core/time_system.h` | WorldTime — хвилини/години/дні/сезони |
| `src/core/overmap.h` | Оверкарта 100×100 |
| `src/render/render.cpp/h` | Ініціалізація текстур, рендер карти/акторів |
| `src/entities/actor.h` | Actor / Player / Enemy / Animal + Race + Body |
| `src/entities/skill.h` | `Skill` enum (зброя+магія) + `SkillLevel` — прогресія, спільна для Actor |
| `src/entities/combat.h` | `resolveAttack()` — формули попадання/урону, `randomHitPart()`/`partName()` |
| `src/entities/magic.h` | `Spell`/`SpellId`/`SpellShape`, `rollSpellCast()`/`resolveSpellHit()` — спільний "двигун" заклять, backfire на низькій навичці. Сам список заклять — у `spells_<school>.h` |
| `src/entities/spells_fire.h` | Заклять Вогню (`inline constexpr Spell`, фрагмент — підключається лише з `magic.h`) |
| `src/entities/spells_water.h` | Заклять Води (той самий фрагмент-патерн) |
| `src/entities/spells_earth.h` | Заклять Землі (той самий фрагмент-патерн) |
| `src/entities/spells_air.h` | Заклять Повітря (той самий фрагмент-патерн) |
| `src/entities/spells_life.h` | Заклять Життя (той самий фрагмент-патерн) |
| `src/entities/spells_death.h` | Заклять Смерті (той самий фрагмент-патерн) |
| `src/entities/item.h` | Item, EquipSlot, ItemType, фабрики предметів |
| `src/entities/npc.h` | `Villager : Actor` — маршрути, домівка, `Occupation`/`bag` |
| `src/entities/corpse.h` | Труп актора після смерті |
| `src/entities/enemy_types.h` | Фабрики типів ворогів |
| `src/ui/inventory.h` | InventoryPanel (UI інвентаря) |
| `src/ui/item_examine_panel.h` | Панель перегляду предмета |
| `src/ui/examine_panel.h` | Перегляд тайла (+ предмета на ньому) |
| `src/ui/cheat_console.h` | Чіт-консоль (backtick) |
| `src/ui/context_menu.h` | Контекстне меню з lambda-діями |
| `src/ui/panel_style.h` | Спільний стиль попапів: `PanelStyle::frame()`/`dimBackdrop()` |
| `src/ui/menu_hub.h` | `MenuHub` — вкладки Character/Body/Effects/Techniques/Magic/Inventory/Craft/Map |
| `src/ui/ui.h` | Панель статів (O) |
| `src/ui/bottom_panel.h` | Лог повідомлень + HUD (HP/SPD/NRG/час, рядок статус-ефектів) |
| `src/ui/body_panel.h` | Стан частин тіла + голод/спрага (B) |
| `src/ui/effects_panel.h` | Панель активних бафів/дебафів (S) |
| `src/ui/skills_panel.h` | Панель навичок — зброя + магічні школи, рівень/прогрес (K) |
| `src/ui/techniques_panel.h` | Панель технік — вимоги/вартість, призначення на слоти хотбара (Y) |
| `src/ui/spells_panel.h` | Панель заклять — вимоги/вартість/ризик невдачі, призначення на слоти хотбара (P) |
| `src/ui/hotbar.h` | Хотбар 1-9 внизу вьюпорту — виклик техніки чи закляття клавішею/кліком |
| `src/ui/wait_panel.h` | Вибір тривалості очікування (T) |
| `src/ui/confirm_panel.h` | Модальний Так/Ні попап (переривання очікування дебафом) |
| `src/ui/craft_panel.h` | Крафт: рецепти, інгредієнти, `countItems`/`consumeItems` |
| `src/ui/trade_panel.h` | Торгівля з NPC-торговцем: купівля/продаж |
| `src/ui/villager_examine_panel.h` | Перегляд інформації про NPC |
| `src/ui/enemy_examine_panel.h` | Перегляд інформації про ворога |
| `src/ui/pickup_panel.h` | Панель підбору предметів з землі |