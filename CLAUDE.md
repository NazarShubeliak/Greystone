# Greystone

## Концепція і ідея

Greystone — це roguelike RPG у фентезі-сеттингу, натхненний **Cataclysm: Dark Days Ahead**, **Kenshi** і **Dwarf Fortress (Adventure Mode)**. Гравець опиняється у живому, жорстокому світі з королівствами, магією та драконами. Немає чітко прописаного сюжету — є відкритий світ, де ти сам вирішуєш що робити і ким стати.

**Головні принципи:**
- ⚖️ **Симетрія світу (фундаментальне правило):** гравець — частина світу, а не його центр. Все що може гравець — можуть NPC, і навпаки: навички, магія, бій, травми, голод, власність, спадщина, кар'єра. Будь-яка механіка пишеться для `Actor`, не для `Player`
- Світ живе без гравця — NPC працюють, торгують, воюють, старіють, одружуються, народжують дітей і помирають
- Виживання важливе: голод, спрага, травми, холод мають значення
- Все має вагу і об'єм — інвентар обмежений фізично
- Смерть — частина гри. Світ зберігається між персонажами; спадщина через заповіти. Зміна персонажів = зміна поколінь
- Прогресія без класів — усе качається через використання (зброя, магія, ремесла)
- ASCII-рендер у стилі класичних roguelike, але з сучасними зручностями (керування мишею, візуалізація шляху)

## GDD документи (дизайн зафіксовано, реалізація попереду)

**`docs/` — джерело правди для планування, не цей файл.** CLAUDE.md документує що вже
зроблено (детально, з причинами рішень) і тримає короткий знімок роадмапу для орієнтиру,
але цей знімок реально відставав від доків раніше (world.md місяцями не потрапляв навіть
у таблицю нижче, хоча лежав у `docs/` від початку) — тому перед тим як планувати нову
фічу чи стверджувати "це не заплановано", **читай сам `docs/*.md`**, особливо секцію
"Порядок реалізації"/"Етапи" в кінці кожного файлу, а не тільки чеклист у "Заплановано".

| Файл | Зміст |
|---|---|
| `docs/magic.md` | 6 базових шкіл (Вогонь/Вода/Земля/Повітря/Життя/Смерть), похідні школи через комбінації (Некромантія = Життя+Смерть, Лід = Вода+Повітря...), створення власних заклять і шкіл, стаміна + час касту, невдалі касти, шляхи навчання (гільдії/книги/фракції/відлюдники) |
| `docs/weapons.md` | 11 навичок зброї (0–100), прокачка через удари, рандомні попадання по 6 частинах тіла з вагами, ефекти типів зброї (меч=кровотеча, булава=оглушення+крізь броню, сокира=sever), техніки як закляття для воїнів (вчителі, вимоги рівня), формули попадання/урону |
| `docs/village.md` | Село як живий організм: сім'ї в будинках, професії із замкнутим економічним циклом, цілі NPC = джерело квестів, вік і життєві стадії, демографія (шлюби/народження/ріст дітей/смерть від старості), спадщина, земля і власність, кар'єра гравця (ферма → староста → король) |
| `docs/world.md` | Генерація світу у два кроки: фізичний світ (висоти/річки/родючість/логічне розселення) → **симуляція історії** (село "проживає" N років до старту гравця: народження/шлюби/смерті/спадщина/могили). Ключова ідея — **один рушій `simulateVillageYear()`** на три застосування: worldgen, подієва симуляція далеких сіл під час гри, прискорена симуляція під час стрибків часу. Екран створення світу зі слайдером років історії, Legends-лог подій. Етапи 1-4 (село → міжсільські зв'язки → фракції/війни → іменні легенди) |

**Ключові рішення з GDD:**
- **Економіка двошарова (як DF):** активна зона = повна предметна симуляція (кожен предмет — реальний Item, фермер фізично сіє/жне/несе в комору, крадіжка забирає реальний хліб); віддалена зона = подієва абстракція (Legends Mode), розгортається назад у предмети коли гравець приходить
- **Час = DF Adventure Mode:** астрономічний, без штучного прискорення віку. Покоління через: worldgen з готовою історією (могили, предки), стрибки часу (сон/подорожі — демографія тикає), зміну персонажів (смерть героя = "retire", новий персонаж застає світ що жив)
- **Навички спільні для зброї і магії:** один enum `Skill`, одна структура `SkillLevel` (0–100, exp), живуть в `Actor` (симетрія). Техніки/закляття на одній панелі хоткеїв
- **Бій:** рандомні попадання (як CDDA/Kenshi, без вибору частини), шанс = 60 + Δdex×3 + навичка/3, урон = зброя × strMult × skillMult(0.5–1.3) ± 20%, броня захищає конкретну частину тіла

---

## Технічний стек

- **Мова:** C++17
- **Рендер:** SDL2 + SDL2_ttf, шрифт DejaVuSansMono 16px
- **Стиль:** ASCII символи на кольоровому фоні
- **Платформа:** Windows (mingw64)

**Команда збірки:**
```
C:/msys64/mingw64/bin/g++.exe -std=c++17 -O2 -o greystone.exe src/main.cpp src/core/astar.cpp src/core/map.cpp src/render/render.cpp -Isrc/core -Isrc/render -Isrc/entities -Isrc/ui -I"C:/msys64/mingw64/include/SDL2" -L"C:/msys64/mingw64/lib" -lmingw32 -lSDL2main -lSDL2 -lSDL2_ttf
```

**Команда збірки headless worldgen-симулятора** (окремий інструмент, не частина гри — див. нижче):
```
C:/msys64/mingw64/bin/g++.exe -std=c++17 -O2 -o worldgen_sim.exe src/tools/worldgen_sim.cpp -Isrc/entities -I"C:/msys64/mingw64/include/SDL2"
```

**Команда збірки Legends Viewer** (окремий інструмент, читає текстовий експорт з реальної гри — команда `legends` у чіт-консолі; жодних заголовків гри чи SDL2 не підключає, тож без `-I` взагалі):
```
C:/msys64/mingw64/bin/g++.exe -std=c++17 -O2 -o legends_viewer.exe src/tools/legends_viewer.cpp
```

`build.sh` — те саме, усі три бінарники (`greystone` + `worldgen_sim` + `legends_viewer`) одним запуском, для bash-оточення (`sdl2-config` замість прямих `-I`/`-L` шляхів mingw64 з команд вище).

---

## Архітектура

### Карта
- 200×200 тайлів, три шари: **TERRAIN** (земля) + **GROUND COVER** (покриття) + **OBJECT** (дерева, каміння)
- Кожен об'єкт має `objectHp` — міцність
- A* пошук шляху з octile heuristic і cost-based рухом

### Світ
- 100×100 секторна оверкарта (`overmap.h`) з біомами
- Детермінований генератор секторів за seed = координати
- Туман війни: відкривається при вході в сектор (3×3 навколо)
- Переходи між секторами при досягненні краю карти

### Актори (`actor.h`)
- `Actor` — базовий клас: позиція, символ, швидкість, тіло, 6 статів, голод/спрага, фракція
- `Body` — 6 частин тіла (голова, тулуб, 2 руки, 2 ноги), кровотеча, переломи, відрубування
- `Player` — 16 слотів екіпіровки, ефективні стати з предметів, інвентар у контейнерах
- `Enemy`, `Animal` — базова AI

### Інвентар (`item.h`, `inventory.h`)
- 16 слотів екіпіровки: HEAD, FACE, NECK, TORSO, OVER_TORSO, HAND_R, HAND_L, HANDS, RING_R, RING_L, BRACELET_R, BRACELET_L, WAIST, LEGS, FEET, BACK
- Предмети мають об'єм (мл) і вагу (г) — контейнер обмежений по обох параметрах
- На землі предмет показується символом категорії (`/`=зброя, `[`=броня, `%`=їжа тощо)
- Прикраси дають бонуси до STR/DEX/CON — реально застосовуються
- Валюта — фізичний предмет "Gold Coin" (`Items::goldCoin()`), використовується в торгівлі з NPC
- Стекування: `Item::count`/`maxStack` (за замовчуванням `maxStack=1` — не стекується). Стекуються валюта (Gold Coin — 100), їжа/питво (~10-20) і сировина (колоди/гілки/каміння — 20). Вільна функція `addToContainer(Item&, Item)` (item.h) — єдина точка додавання в контейнер: мерджить в існуючий стек, розбиває на кілька стеків при перевищенні ліміту, все-або-нічого (не лишає предмет "застряглим" напівдоданим)

### Взаємодія зі світом
- Правий клік на сусідній об'єкт → Chop/Mine/Harvest
- Удар = 20 + (STR-10)*2 пошкодження
- При знищенні падають ресурси: Logs, Branches, Stone, Berries

---

## Поточний стан (що реалізовано)

- [x] Карта, FOV, A* з cost-based рухом
- [x] Енергетична покрокова система (CDDA-стиль)
- [x] Ієрархія акторів з частинами тіла і кровотечею
- [x] Система рас з модифікаторами статів
- [x] Оверкарта з біомами і туманом війни
- [x] Переходи між секторами
- [x] Чіт-консоль (reveal_map, hide_map, tp X Y)
- [x] Інвентар: 16 слотів, контейнери, підбір/викидання/екіпіровка
- [x] Перегляд предметів (на землі і в інвентарі)
- [x] Шкали голоду/спраги у Body Panel
- [x] Панель характеристик з бонусами від екіпіровки і бойовими статами
- [x] Захист від броні зменшує вхідний урон
- [x] Зброя в руці додає урон
- [x] Взаємодія зі світом: рубати дерева, добувати каміння, збирати кущі
- [x] Система сіл: ~15 сіл на оверкарті (маркер `#` золотий), будівлі з стінами/підлогою/дверима, бруд-доріжки до центральної площі з криницею
- [x] Логічна генерація села: 2 ферми на випадкових сторонах світу (одна може стати Herbalist-полем), Коваль/Староста/Лісник — окремі будівлі, розташовані випадково навколо площі (Староста — ближче, з перевіркою накладання), з декором під професію (ковадло, штабелі колод). `map.cpp` вирішує роль будівлі під час генерації (`villageBuildings`), `spawnVillagers()` лише читає це
- [x] Система часу: WorldTime (хвилини/години/дні/сезони), день/ніч впливає на FOV і темряву
- [x] NPC-жителі: маршрути (WANDER/WALK_HOME/SLEEP), домівки/ліжка, привітання, Examine
- [x] Лут з ворогів: `dropEnemyLoot()` кидає предмети + лишає труп при смерті
- [x] Крафт з ресурсів: `CraftPanel`, рецепти зі списком інгредієнтів, переривання зберігає частковий предмет (resumable)
- [x] Очікування/відпочинок: `WaitPanel` (T) — прискорення часу до цілі, переривається ворогом поруч або погіршенням дебафу голоду/спраги (`ConfirmPanel`)
- [x] Панель ефектів (S): список активних бафів/дебафів (голод, спрага, кровотеча, бонуси від прикрас, світло) з поясненням
- [x] Система професій: `Occupation` (Farmer/Herbalist/Woodcutter/Blacksmith/Elder/Seamstress) — Farmer/Blacksmith/Elder/Woodcutter обов'язкові в кожному селі, Herbalist часто, Seamstress рідко
- [x] Староста говорить інакше за звичайних жителів: окремі `ELDER_GREETINGS_DAY/NIGHT` (main.cpp) замість загальних `GREETINGS_DAY/NIGHT` — авторитетний тон, а не звичайна балачка. Квести від старости — ще заплановано
- [x] Торгівля з NPC: кожен житель з професією продає своє з власної сумки (`bag`, реальний контейнер — `goodsFor()` наповнює його при спавні), `TradePanel` — купівля/продаж за фізичну валюту "Gold Coin"
- [x] Авто-повтор рубання/копання: правий клік продовжує бити ціль до знищення
- [x] `Villager` — повноцінний `Actor` (як Player/Enemy): статblock, 6-частинне `Body`, `hunger`/`thirst`/`tickNeeds()`/`takeDamage()` — усе успадковане, нічого не дублюється. Потреби: голод/спрага як у гравця, реальна їжа зі своєї сумки (`bag`, contents — жодного "невидимого" інвентаря); Farmer/Herbalist поповнюють запас збором стиглого врожаю зі свого поля, решта професій — ні (ризик голоду). Стани `EAT`/`DRINK` — йдуть додому їсти чи до криниці пити. NPC може померти від голоду/спраги (через `takeDamage()` → `Body::isDead()`), при смерті лишає труп і скидає весь вміст сумки (`dropBag()`)
- [x] `Enemy::bag` — той самий підхід: замінено "невидимий" `carried` на реальний контейнер (`Items::backpack()`), `weaponDmg()` і "with X" повідомлення читають з нього. Гобліни/орки/бандити мають раціон (їжа+вода) у сумці; скелети — ні (нежить). Голод/спрага у ворогів тепер реально вбивають (раз на перетнуту ігрову годину), а не лише кровотеча. `dropBag()` (спільний хелпер) розкидає весь вміст + порожню сумку при смерті — і в `Enemy`, і в `Villager`
- [x] Fullscreen: гра стартує в borderless fullscreen desktop під роздільність монітора; `Alt+Enter` перемикає у віконний режим і назад (`applyScreenSize()`/`toggleFullscreen()`, main.cpp)
- [x] Єдиний візуальний стиль попап-панелей (`panel_style.h`, `PanelStyle::frame()`/`dimBackdrop()`) — Trade/Examine/Craft-confirm/Pickup/Wait/Confirm використовують спільну рамку/фон/заголовок замість кожен свого
- [x] Меню-хаб (`menu_hub.h`, `MenuHub`) — Character/Body/Effects/Skills/Inventory/Craft/Map об'єднані в одне повноекранне вікно зі смугою вкладок зверху; перемикання гарячими клавішами (O/B/S/K/I/C/M — працює навіть з середини іншої вкладки) або кліком по вкладці, кнопка закриття (X) в кутку
- [x] Бойова система жителів: атака (лівий клік/контекстне меню — нейтральних тільки впритул, `isHostile()`-гейт від випадкових вбивств), реакція `FIGHT`/`FLEE` за професією + наявністю зброї в сумці (`rollCombatReaction()`), свідки в радіусі бачення теж реагують (`villagerReactToAttack()`), боєць із низьким HP ламається і тікає — той самий патерн, що `Enemy::wantsToFlee()`
- [x] "Підійти, потім щось зробити" — атака й розмова з жителем здалеку спершу підводять гравця впритул (chase-to-attack патерн з Enemy, поширений на `attackTargetVillager`/`talkTargetVillager`, шлях перераховується щокроку якщо ціль рухається)
- [x] Одяг жителів (`Villager::outfit`, окремо від `bag`) — завжди одягнені, одяг падає при смерті разом з рештою лута
- [x] Трупи й предмети на землі прив'язані до сектора (`Corpse`/`GroundItem::sectorX/sectorY`) — більше не зникають при виході з сектора; фільтруються за поточним сектором при рендері/взаємодії (координати 0-199 повторюються в кожному секторі)
- [x] Робочий діалоговий режим (`PanelMode::DIALOGUE`, `BottomPanel`) — "Talk" показує репліку жителя й список відповідей (Trade/Ask about their work/Farewell) у нижній панелі замість одного рядка в лог. Не повне дерево — один рівень глибини, "Ask" перебудовує той самий список
- [x] Фундамент бойової системи (`skill.h`, `combat.h`) — `Skill` enum (зброя+магія) + `SkillLevel` (0–100, exp, прокачка промах+1/влучання+2/вбивство+5) живуть у **Actor** (не Player) — кожен ворог і житель має власні навички. `weaponSkill`/`sharp` на `Item`, проставлено на всій реальній зброї. `combat.h::resolveAttack()` — єдина формула попадання/урону (шанс = 60+Δdex×3+навичка/3, множник урону 0.5–1.3 від навички, ±20% розкид, head×1.5) замінила 8 копіпастних блоків у `main.cpp`; тепер існують промахи (їх не було). Ворожі фабрики (`enemy_types.h::trainSkill()`) дають стартовий рівень під зброю яку носять — Goblin Dagger 15-20, Skeleton Mace 20-28, Wolf Unarmed 15-25, Orc/Bandit 25-35 (кожен рандом у діапазоні); жителі лишаються 0 (не бійці). Ефекти типів зброї (кровотеча/оглушення/sever), техніки і магія — ще не зроблені
- [x] Панель навичок (`skills_panel.h`, `SkillsPanel`, хоткей K) — вкладка в MenuHub, показує всі 17 `Skill` гравця (зброя ліворуч, магічні школи праворуч) з рівнем і прогрес-баром до наступного. Магічні школи показують рівень 0, поки немає кастування — очікувано
- [x] Ефекти типів зброї (частково, `combat.h`/`actor.h`) — **меч**: гарантована кровотеча на будь-якому влучанні (не тільки при важкій рані), **сокира**: підвищений шанс sever при добиванні (65% замість базових 40%), **булава**: ігнорує 50% захисту захисника, **кинджал**: ×1.5 урону по цілі що не помітила атакуючого (наразі підключено лише для сплячого жителя — `Villager::State::SLEEP`). Свідомо не зроблено: оглушення булавою (пропуск ходу — немає механіки stun/turn-skip), швидша дія кинджалом (усі дії коштують фіксовані 100 енергії), спис на 2 тайли, ефекти кулаків/щита
- [x] Лікування кровотечі (`Item::curesBleed`, `herbalPoultice`) — гарантована кровотеча мечем була б нечесною без ліків (`bleed` ніде природно не спадав), тому "Use" на Herbal Poultice тепер реально скидає кровотечу з усіх частин тіла гравця, а не просто мала опис-обіцянку. Поки що тільки гравець — NPC не лікують себе самі (симетрія тут не повна, свідомо відкладено)
- [x] Стаміна (`Actor::stamina`/`maxStamina`) — `maxStamina = 50 + constitution×5`, регенерує +3/хід через `tickNeeds()` (той самий виклик що й у голоду/спраги, тож і ворог, і житель регенерують так само). `hasStamina()`/`spendStamina()` готові як інтерфейс — нічого ще не витрачає стаміну, це прийде з першою технікою/закляттям. Показ у HUD (`bottom_panel.h`, STA: поруч з NRG)
- [x] Перші техніки (`combat.h::Technique`/`TechniqueId`) — **Brutal Strike** (Сокира 20, гарантована глибока рана+кровотеча), **Lunge** (Меч 30, +25 до шансу влучання), **Backstab** (Кинджал 40, ×2 урону, лише проти сплячого жителя — для ворогів немає системи "чи помітив"). Коштують стаміну + додатковий час понад звичайний удар (`onPlayerAct(extraEnergy)`). З'являються в правоклік-меню атаки лише коли рівень навички під поточну зброю в руці досягнутий (без окремого "вивчено від вчителя" — це наступний крок). Атака технікою вимагає вже стояти впритул — авто-підходу як у звичайної "Attack" ще нема
- [x] Чіт-команди для тестування навичок/технік (`cheat_console.h`) — `setskill <skill> <0-100>` виставляє будь-яку з 17 навичок напряму (`SKILL_TOKENS`, порядок мусить збігатись з enum `Skill`), `unlocktech <name>` піднімає навичку до мінімального рівня потрібної техніки (`TECH_TOKENS`, порядок = enum `TechniqueId`) — не окремий "unlocked"-прапорець, той самий гейт що й у грі
- [x] Панель технік + хотбар (`techniques_panel.h`, `hotbar.h`, хоткей Y) — вкладка Techniques в MenuHub показує всі техніки (розблоковані/заблоковані, вимога, вартість стаміни/часу) і 9 квадратиків для ручного призначення на слот (клік — призначити, повторний клік по тому самому слоту — зняти). Хотбар з 9 слотів завжди на екрані під час геймплею (ховається лише коли відкритий сам хаб), поверх нижньої частини вьюпорту карти. Клавіші 1-9 і клік мишкою по квадратику викликають техніку зі слоту — авто-таргетить першого живого ворога впритул, потім першого жителя впритул (той самий вибір цілі що й "Attack" в контекстному меню). Порожній слот / незасвоєна техніка / брак стаміни — повідомлення в лог, дію не витрачено
- [x] Хотбар-прицілювання узагальнено на техніки й закляття (`techniqueTargeting` → `hotbarTargeting`, `main.cpp`) — один режим прицілювання на обидва типи дій, замість дублювання
- [x] Чотири закляття школи Вогню (`magic.h`, вкладка Magic/хоткей P) — **Spark** (Fire 0, `SpellShape::POINT`, слабкий снаряд), **Fireball** (Fire 15, `SpellShape::BURST` радіус 1 — вибухає навіть якщо не влучив точно в ціль), **Wall of Fire** (Fire 20, окрема multi-tile-select система — див. окремий пункт нижче), **Explosion** (Fire 35, `SpellShape::BURST` радіус 2). Кожен спел б'є **тайл, не актора** — клік ніколи не скасовується через "нема цілі" (`useSpellAtTile()`, `main.cpp`): POINT чіпає лише сам клікнутий тайл, BURST будує footprint по радіусу й чіпає кожного ворога/жителя в ньому; на порожньому тайлі Fire-заклинання кидає кубик спершу підпалити флаговану сцену (`tryIgniteObject()`, 30% — дерева/сухостій/кущі/пшениця/пень, реально б'є по `objectHp` і при знищенні валить об'єкт через `destroyWorldObject()`, той самий лут-шлях що й рубання/копання), а якщо об'єкта нема — траву/мох/листя (`tryIgniteGround()`, 30%, `GroundId::G_SCORCHED`, суто косметичний свап ground cover — без розповсюдження/DoT для жодного з двох). `destroyWorldObject()` — витягнутий з `interactWithObject()` спільний шматок (свап тайла + лут + повідомлення), тепер один код на знищення об'єкта для сокири/кайла і для вогню. `SpellShape::BURST`-заклинання (Fireball/Explosion) також малюють тьмяний спалах по всьому радіусу вибуху (`SpellBurst`/`renderSpellBurst()`, `main.cpp`) — з'являється з затримкою що збігається з польотом снаряда (`SpellProjectile::duration`), потім гасне; суто косметика, урон уже пораховано миттєво при каст-резолві. Бекфайр і приріст навички рахуються раз на каст (`rollSpellCast()`), окремо від per-target урону (`resolveSpellHit()`), щоб Explosion по натовпу не кидав кубик невдачі і не качав скіл кілька разів. Каст = стаміна + додатковий час (`onPlayerAct(extraEnergy)`), націлювання — та сама система що й техніки (`hotbarTargeting`), тільки без вимоги "стояти на акторі" — техніки (мілі) досі вимагають ціль під курсором, спели ні. Слоти хотбара кодують і техніку, і закляття в тому самому `int[9]` через offset (`Hotbar::SPELL_SLOT_OFFSET`). `unlockspell <name>` у чіт-консолі за зразком `unlocktech`. Свідомо не зроблено: Тепло (немає системи температури/холоду взагалі), Вогняний щит (реактивний тригер на вхідний удар — немає хука), Метеор (затримка/попередження перед вибухом — немає механізму відкладеної дії), Імунітет до вогню (немає типізації урону — `takeDamage()` бере просто `int`), пасивні заклинання (жодного поки нема), інші школи
- [x] Wall of Fire — перший справжній tile-hazard у грі (`magic.h`/`main.cpp`) — гравець замість одного кліку **сам вибирає кілька тайлів** у радіусі (`Spell::manualArea`, окремий режим прицілювання `wallTargeting`/`wallTargetSlot`/`wallSelection` — мутуально ексклюзивний з `hotbarTargeting`): лівий клік додає/знімає тайл (`toggleWallTile()`, той самий toggle що й `Hotbar::assign()`), Enter підтверджує каст (`confirmWallTargeting()`), правий клік/Esc скасовує без витрат. Вартість — **за тайл** (`Spell::staminaCost` тепер per-tile для `manualArea`, повідомлення після кожного кліку показує поточну суму), тому більше тайлів = дорожче, як просив користувач. При підтвердженні: один бекфайр-ролл на весь каст (`rollSpellCast()`), потім кожен позначений тайл або б'є актора що там стоїть (`resolveSpellHit()`, включно з гравцем якщо позначив свій тайл), або шкодить об'єкту/траві (`applySpellObjectDamage()`/`tryIgniteGround()`), і в будь-якому разі отримує `FireHazardTile` (новий глобальний `fireHazards`, `igniteFireHazardTile()` — рефреш таймера якщо тайл вже горів, а не дублікат) на `hazardTurns` ходів (Wall of Fire: 4). `tickFireHazards()` — раз на дію гравця (`onPlayerAct()`, той самий каданс що й `tickEnemyNeeds()`), палить `baseDamage` кожному хто стоїть на активному тайлі (гравець/ворог/житель — симетрично), і зменшує лічильник, знімаючи тайл коли він добіг до нуля. Рендер: `renderFireHazards()` — мерехтливий заливка (альтернуючий alpha через `SDL_GetTicks()`) під акторами, малюється одразу після `renderMap()` як шар землі; `renderWallTargeting()` — прев'ю під час вибору (тьмяний тон = у радіусі, яскравий колір заклинання = вже позначено). Свідомо не зроблено: вогонь не розповсюджується на сусідні тайли сам, немає "хто підпалив" атрибуції для репутації/свідків (крім `villagerReactToAttack()` при прямому влученні)
- [x] Магія реально шкодить об'єктам і власному касту — **`Material`** (`terrain.h`: NONE/WOOD/STONE/METAL, нове поле `ObjectDef::material`, проставлено всім 21 об'єктам) + **`materialResistance(Skill, Material)`** (`main.cpp`) визначає скільки урону школа реально завдає матеріалу — зараз тільки Fire: дерево ×1.6, камінь ×0.15, метал ×0.4, все інше (і будь-яка майбутня школа) ×1.0 за замовчуванням. **`applySpellObjectDamage()`** замінив старий `tryIgniteObject()` — тепер це детермінований урон (не шанс 30%) по **будь-якому** об'єкту в footprint будь-якого заклинання (не тільки Fire — "люба магія" per user), не тільки "горючому" списку: стіна від фаєрбола реально втрачає HP і при знищенні валиться каменем (`destroyWorldObject()`, новий кейс `O_WALL` — раніше стіна взагалі не давала лута навіть від кайла, це заразом і фікс). Об'єкти з `durability<=0` (криниця) — недоторканний сентінел, постріл поглинається без ефекту, як і при звичайній взаємодії. **Гравець тепер теж ловить власний вибух** — `useSpellAtTile()`/`confirmWallTargeting()` перевіряють чи `(tx,ty)` з footprint збігається з позицією гравця, і якщо так — `resolveSpellHit(player, player, id)` б'є самого себе тим самим розрахунком, що й ворога (раніше кинути фаєрбол собі під ноги було безкарно). Свідомо не зроблено: material-таблиці для Water/Earth/Air/Life/Death (їх ще нема), явна протидія типу "лід проти вогню"
- [x] Перша школа після Вогню — Вода (`magic.h`/`main.cpp`) — **Create Water** (Water 0, новий тип закляття `Spell::selfCast` — миттєво п'є `player.thirst` без жодного прицілювання/кліку, `useSelfSpell()`) і **Water Jet** (Water 0, `SpellShape::POINT`, той самий патерн що Spark). Перше `selfCast`-закляття в грі: `useHotbarSlot()` розпізнає `s.selfCast` і одразу резолвить на самому касторі, минаючи `hotbarTargeting`/`wallTargeting` цілком — стамина й бекфайр-ролл (`rollSpellCast()`) все одно рахуються, тільки нема "точки удару". Water-заклинання гасять активні тайли Wall of Fire (`tryExtinguishFire()`, викликається для кожного тайла footprint незалежно від того що там ще є — можна одночасно й погасити вогонь, і вдарити ворога що в ньому стоїть) — перша міжшкільна взаємодія в грі. Заразом узагальнив формулювання бекфайру ("fizzles and burns" → "backfires, hurting") — старий текст був специфічно про вогонь, для Water не пасував. Свідомо не зроблено: Учень/Адепт/Майстер рівні Води (Уповільнення, Дощ, Заморозка — Заморозка додатково вимагає Повітря)
- [x] Заклять-таблиці розбито по школах (`spells_fire.h`/`spells_water.h`/`spells_earth.h`/`spells_air.h`) — на прохання користувача, щоб список заклять не звалювався в один суцільний масив в `magic.h` в міру росту шкіл. Кожен файл — фрагмент (`inline constexpr Spell NAME_SPELL = {...}`), підключається лише з `magic.h` одразу після оголошення `Spell`/`SpellShape` (щоб уникнути циклічного інклюда — фрагменти НЕ інклудять `magic.h` самі), `spellInfo()` збирає їх у фінальний масив за `SpellId`. Уся логіка кастування (`useSpellAtTile`, `confirmWallTargeting`, `tickFireHazards`, `materialResistance` і т.д.) свідомо лишилась у `main.cpp` — окремий винос обговорювався, але відкладено (вищий ризик через extern-залежності від `player`/`enemies`/`villagers`/`panel`)
- [x] Перша школа Землі — Stone (`spells_earth.h`) — Earth 0, `SpellShape::POINT`, той самий патерн що Spark/Water Jet. Skin Hardening (другий Novice-спел Землі, тимчасовий бонус захисту) свідомо відкладено — потребує buff-duration системи, якої в грі ще нема (та сама, що відклали для Fire Shield)
- [x] Перша школа Повітря — Gust (`spells_air.h`) — Air 0, `SpellShape::POINT`, найслабший урон з усіх заклять (докс: "найслабший урон але найкращий контроль"), плюс новий ефект `Spell::knockback` — після удару відштовхує ціль на 1 тайл у напрямку від каста (`tryKnockback()`, `main.cpp`, generic над `Actor&` — працює для ворога/жителя/гравця симетрично), мовчки не спрацьовує якщо клітинка позаду зайнята/непрохідна/за межами карти. Заразом виправив баг: повідомлення про смерть від закляття завжди казало "dies, charred" незалежно від школи (лишилось з часів коли існував лише Вогонь) — тепер `", charred"` тільки для Fire, інші школи просто "dies." (стосується `useSpellAtTile()`; `confirmWallTargeting()` не чіпав — Wall of Fire завжди вогонь, там "charred" і далі коректний). Легкість (другий Novice-спел Повітря, тимчасовий бонус швидкості) свідомо відкладено — та сама buff-duration система
- [x] Панель заклять згрупована по школах (`spells_panel.h`) — на прохання користувача: зліва сайдбар шкіл (той самий left-list/right-content патерн що в `InventoryPanel`), клік перемикає `selectedSchool`, справа тільки закляття цієї школи. Список шкіл динамічний (`availableSchools()` — збирає унікальні `Spell::school` з `spellInfo()`, а не хардкоджений масив), тож нова школа з'являється в сайдбарі сама, щойно в неї є хоч одне закляття
- [x] По одному новому закляттю для кожної існуючої школи (окрім Вогню — там усе, що не вимагає нової системи, вже зроблено) — обирав те, що лягає на існуючу архітектуру без нової інфраструктури:
  - **Rain Call** (Water 15, `spells_water.h`) — новий тип `Spell::rainCall`: чистий utility-BURST, не б'є нікого, гасить усі активні тайли `fireHazards` в радіусі й змиває `G_SCORCHED` назад у `G_GRASS` (той самий `tryExtinguishFire()` з Water Jet, просто по площі)
  - **Stone Wall** (Earth 15, `spells_earth.h`) — новий тип `Spell::buildsWall`: єдине закляття що редагує тайл мапи напряму (докс: "єдина школа що редагує тайли карти") — ставить `O_WALL` на чистій прохідній клітинці в радіусі; провалюється (з витратою касту) якщо клітинка зайнята актором/об'єктом
  - **Vortex** (Air 30, `spells_air.h`) — нуль нового коду: просто `SpellShape::BURST` + вже існуючий `Spell::knockback` разом — відкидає всіх у радіусі, той самий шлях що й Gust
  
  Обидва нові типи (`rainCall`/`buildsWall`) резолвляться окремими гілками на початку `useSpellAtTile()`, до звичайної footprint/hit-логіки — повністю обходять `resolveSpellHit()`
- [x] Візуальна відмінність Gust/Vortex + анімація відкидання — Gust (`>`, крижано-блакитний) і Vortex (`@`, пилюжно-жовтий) мали майже однаковий символ/колір, важко було зрозуміти що спрацювало (user feedback). Заразом додав сам ефект knockback — раніше ціль просто мовчки телепортувалась на сусідній тайл. **`KnockbackFlash`** (`main.cpp`, вектор — не один слот як `SpellBurst`, бо Vortex може відкинути кількох одразу) — гліф самої цілі (`Actor::symbol`/`color`) ковзає зі старої клітинки на нову за ~150мс, `tryKnockback()` сам викликає `spawnKnockbackFlash()` перед переміщенням, `renderKnockbackFlashes()` малює й вичищає протухлі
- [x] Три нові закляття Землі поверх Stone Wall (усі в `spells_earth.h`), на прохання користувача. Дальність (`range`) поодиноких заклять зменшено на прохання користувача: **Stone Wall** і **Reclaim Wall** тепер `range=2` (було 5 — впритул/на кілька кроків, не через пів карти). **Architect** лишився `range=6` (пробували 3, повернули назад) — багатотайловому будівництву потрібен ширший захват. Wall Throw теж `range=6` для самого кидка (тільки радіус вибору стіни зменшено, дивись нижче):
  - **Reclaim Wall** (Earth 20) — новий тип `Spell::reclaimsWall`: прибирає стіну на клікнутій клітинці, але **тільки якщо там `O_WALL` конкретно** (не просто `Material::STONE` — перевірка по `ObjectId`), щоб закляття Землі не чіпало дерев'яні об'єкти які випадково поділяють матеріал. Без лута — стіна "тоне назад у землю", не розсипається каменем
  - **Wall Throw** (Earth 25) — новий тип `Spell::throwsWall`: б'є ціль важким уроном (18 базового — найбільше з POINT-заклять) + `knockback=true` (стіна що летить у тебе має відкидати). Спочатку робив auto-pick найближчої стіни до гравця, але користувач попросив вибір вручну — тепер **двофазне прицілювання** (`wallThrowTargeting`, окремий режим від `hotbarTargeting`/`wallTargeting` — жоден із двох не підходив, потрібні рівно два кліки з різними правилами валідності): перший клік вибирає яку `O_WALL` кинути (радіус вибору `WALL_THROW_PICK_RADIUS = 3` навколо гравця — "піднімаєш зблизька", менший за `s.range = 6`, дальність прицілу по цілі — "кидаєш дальше"), другий — куди кинути (звичайний `s.range`). Невалідний клік на будь-якій фазі скасовує все, а не перепитує. `castWallThrow()` — сам ефект (той самий що раніше сидів у `useSpellAtTile()`, просто тепер викликається з `resolveWallThrowClick()` після другого кліка); `renderWallThrowTargeting()` — прев'ю: фаза 1 підсвічує стіни в радіусі пошуку червоним, фаза 2 — звичну ціль-підсвітку
  - **Stone Wall тепер без "кидка"**: прибрав `spawnSpellProjectile` для `buildsWall`/`reclaimsWall`/`throwsWall` (перші два — не мають літючого об'єкта взагалі, `throwsWall` спавнить свій власний з позиції стіни) — замість польоту тепер миттєвий `spawnSpellBurst(tx,ty,0,color,0)` — спалах радіусом 0 без затримки прямо на цілі, виглядає як "стіна сама піднялась", а не "я щось кинув" (user feedback)
  - **Architect** (Earth 50, Master, `Spell::manualBuild`, радіус вибору тайлів 6) — та сама multi-tile-select система що й Wall of Fire (`wallTargeting`/`confirmWallTargeting()`, `useHotbarSlot()` тепер пускає в цей режим і `manualArea`, і `manualBuild`), тільки замість `FireHazardTile` кожна чиста позначена клітинка отримує `O_WALL`. Вартість — за тайл, як і Wall of Fire
- [x] Buff-система (нарешті) — тричі відкладали (Fire Shield, Skin Hardening, Lightness чекали на неї ще з сесій Fire/Earth/Air). Прості лічильники ходів прямо на `Actor` (`actor.h`) — не generic buff-list, бо їх мало, довідникова структура була б складнішою за те що вона замінює. Тикають в `tickNeeds()` (той самий каданс що голод/спрага/стаміна-реген — раз на дію актора, працює однаково для гравця/ворога/жителя за принципом симетрії). `Spell::buffTurns` — скільки ходів триває ефект; для self-cast бафів `useSelfSpell()` вирішує який лічильник виставити за `SpellId` (той самий підхід що й `CREATE_WATER`), для Slowness (не self-cast — цільовий дебаф) новий `Spell::slows` виставляє його на **цілі** прямо в бойовому циклі `useSpellAtTile()`, поруч з `knockback`:
  - **Fire Shield** (Fire 30, `spells_fire.h`) — поки активний, відбиває третину вхідного мілі-урону назад нападнику. Хук прямо в `combat.h::resolveAttack()` (новий `AttackResult::reflectedDamage`) — працює симетрично для будь-якої пари атакер/захисник, хоч зараз реально спрацьовує лише коли ворог/житель б'є гравця (бо тільки гравець кастує заклинання). `enemyAct()`/`villagerCombatAct()` (`main.cpp`) показують повідомлення й обробляють смерть нападника від відбитого урону (`dropEnemyLoot`/`dropVillagerLoot`, якщо відбилося смертельно)
  - **Skin Hardening** (Earth 0, `spells_earth.h`) — +8 захисту, хук у `Player::totalDefense()`. Поки тільки гравець — у ворогів/жителів нема еквівалентного розрахунку захисту щоб зачепитись
  - **Lightness** (Air 0, `spells_air.h`) — +15 ефективної швидкості, хук у `tickWorld()` для гравця й ворогів (у жителів окрема система руху, не energy/speed — хук поки не для них)
  - **Slowness** (Water 15, `spells_water.h`) — цільовий дебаф (не self-cast): POINT-удар слабким уроном + `-15` ефективної швидкості цілі (`slowedTicks`, docs: "Мокрий ворог повільніший"). Той самий `tickWorld()`-хук що й Lightness, тільки віднімає замість додає
  - **Acceleration** (Air 30, `spells_air.h`) — сильніший і коротший варіант Lightness (`+30`/15 ходів проти `+15`/25), окремий лічильник `accelTicks` бо величина бонусу різна. Докс описує це як "додатковий хід" (буквально зайва дія за той самий такт) — це вимагало б перебудови самого turn-loop, тому свідомо спрощено до сильнішого спід-бафа замість чесно реалізованого зайвого ходу
  - Усі п'ять відображаються в Панелі ефектів (S) з лічильником ходів що лишились (`effects_panel.h`)
- [x] Школи Життя і Смерть — останні дві базові школи, обидві з одним Novice-заклинанням (той самий "почати з малого" патерн що й Water/Earth/Air), кожна у своєму `spells_<school>.h`:
  - **Minor Heal** (Life 0, `spells_life.h`) — self-cast, лікує 15 HP торсу гравця (той самий "завжди торс" підхід що вже використовують голод/спрага/кровотеча для недирекційного HP-урону — тепер і для HP-відновлення). Перше лікування в грі взагалі — раніше навіть Herbal Poultice лікував тільки кровотечу, не HP напряму
  - **Withering** (Death 0, `spells_death.h`) — звичайний POINT-урон, той самий патерн що Spark/Water Jet/Stone/Gust. Докс описують це закляття як таке що ігнорує броню — насправді це вже неявно правда для **будь-якого** закляття (`resolveSpellHit()` ніколи не віднімає захист цілі, на відміну від `resolveAttack()` для мілі), тому це не є реальною відмінністю поки що
  - Sense Life/Sense Death (другі Novice-закляття обох шкіл — бачити живих/мертвих крізь стіни) свідомо відкладено — потребують reveal-механіки, якої в грі ще нема
- [x] Розширені символи (CP437-стиль) — рендер перемкнуто з `TTF_RenderText_Solid` (Latin-1) на `TTF_RenderUTF8_Solid` у всіх ~25 файлах, що малюють текст, тож звичайний DejaVu Sans Mono (без заміни шрифту) тепер коректно показує символи поза ASCII. **Wall** — `█` (FULL BLOCK, було `#`, `terrain.h`) — підтверджено, виглядає добре: Block Elements (U+2580-259F) шрифт має. **Villager/Bandit** — пробував `☺` (WHITE SMILING FACE, U+263A), але DejaVu Sans Mono, вочевидь, НЕ має гліфа з блоку Miscellaneous Symbols (♥♦♣♠☺☻ і т.д.) — рендерилось криво, повернуто назад на `"@"` для обох. `renderVillagers()` тепер реально читає `v.symbol` замість хардкодженого `"@"` (був баг — лишив цей фікс, він не залежить від того, який символ у `v.symbol`). Символи вкладаються прямо як UTF-8 літерали в C-рядки (`const char* symbol`) — mingw g++ пропускає їх байт-в-байт. Заплановано (не зроблено): або підібрати "людський" символ з Geometric Shapes/Box Drawing (блоки, які шрифт точно підтримує), або поставити окремий CP437 bitmap-шрифт для повного автентичного вигляду з ♥♦♣♠☺
- [x] Справжня сім'я замість геометричного гурту ліжок (`npc.h`, `main.cpp::spawnVillagers()`) — до цього "домогосподарство" було просто ліжками в радіусі 8 тайлів зі спільним прізвищем, без жодних реальних зв'язків і з повністю незалежним віком кожного жителя. Тепер `Villager` має `spouseId`/`motherId`/`fatherId`/`childIds` (індекси у `villagers`, стабільні на весь час життя села — жителі ніколи не видаляються з вектора, лише позначаються `!alive`, і весь вектор перебудовується разом при зміні сектора):
  - **Подружжя**: будь-яке дволіжкове домогосподарство (ферма/травниця/староста) лінкується `spouseId` в обидва боки; другий вік більше не незалежний рандом — перекидається близько до першого (`generateAge(HUMAN, age±10)`) замість двох випадкових дорослих 18-80
  - **Діти**: лише ферма/травниця (одружена пара), 60% шанс на 1-3 дітей, вік молодший за молодшого з батьків мінімум на 16 років (`maxChildAge = min(parent ages) - 16`, пропускається якщо не влазить). Дитині нема окремого ліжка (`map.cpp` ставить рівно 2 ліжка на будинок — свідома архітектурна межа), тому вона ділить домашній тайл з батьком, шукаючи вільну сусідню прохідну клітинку в радіусі 2 щоб не стакатись з батьками/іншими дітьми. `occupation = NONE`, власний маленький запас їжі (3 хлібини) — та сама спрощена модель "нема комори", кожен житель носить свою їжу
  - `VillagerExaminePanel` показує вік і рядки Spouse/Parents/Children (імена резолвляться в `main.cpp` в момент побудови контекстного меню "Examine", бо панель зберігає лише копію `Villager`, а не посилання на живий вектор)
  - Свідомо не зроблено (наступний крок з `village.md`): брати/сестри між домогосподарствами, вдівці/повторний шлюб, спадщина, "сліди історії" (могили — нема навіть типу об'єкта), і сама демографія — ніхто не старіє, не одружується і не народжується під час гри, все лише worldgen-знімок при заселенні села
- [x] Комора (`npc.h`, `main.cpp::spawnVillagers()`) — реальний спільний контейнер їжі на сім'ю замість того щоб кожен житель тягнув усе в особистій сумці. Фізично це вже існуюча декоративна бочка (`O_BARREL`), яку `map.cpp` й так ставить у кожній фермі/травниці — `findNearbyBarrel()` шукає найближчу до ліжка господаря (радіус 12, надійно влучає у "свою" бочку, а не в бочку коваля/лісника — ті стоять в геть іншій будівлі far away). Тільки фермерські/травницькі домогосподарства мають комору; власне `Item` (новий `Items::grainBarrel()`, велика ємність, без слота екіпіровки — не носиться) лежить лише на `Villager` головного робітника, а `granaryOwnerId` (виставляється і господарю, і подружжю, і кожній дитині) — спільний покажчик на неї, той самий патерн що й `spouseId`/`childIds`. Пріоритет їжі при `State::EAT` тепер триступеневий (`eatFrom()` — спільна лямбда): 1) власна сумка, 2) сімейна комора через `granaryOwnerId`, 3) лише для фермера/травниці — збір стиглого врожаю. Заразом виправив приховану спрощеність: збір врожаю раніше просто скидав голод у 0 без жодного `Item` (суперечило принципу "жодного невидимого інвентаря"); тепер урожай реально кладе 3 порції їжі (`Items::flatbread()`/`Items::mushroomStew()`, залежно від професії — ті самі предмети що продає торговець) в комору через `addToContainer()`, і одну з них одразу з'їдає — 2 лишаються сім'ї. Стартовий запас комори — 8 порцій при спавні. Свідомо не зроблено: гравець не може відкрити/оглянути бочку-комору як контейнер (декоративні бочки коваля/лісника й досі неінтерактивні, тож це узгоджено з рештою гри, а не half-finished) — це наступний крок якщо знадобиться; сама "економіка предметами" (сіє→жне фізично, торгівля між NPC) — окремий пункт нижче, досі не зроблено. **Доповнення:** `transferGranary()` (`main.cpp`, викликається на початку `dropVillagerLoot()` — спрацьовує для будь-якої причини смерті) — при смерті власника комори передає її подружжю, інакше першій живій дитині; без спадкоємців вміст висипається на землю замість того щоб зникнути назавжди (перший маленький шматок "Спадщини NPC" з роадмапу нижче, актуалізувався коли з'явилась смерть від старості)
- [x] Вік і стадії життя (`actor.h`, `combat.h`, `main.cpp`, roadmap з `village.md`) — `LifeStage` (Child/Teen/Young Adult/Adult/Elder/Old), межі — частки `minAge`/`maxAge` раси (не хардкод людських років), тож масштабується з 80-річної людини на 700-річного ельфа; безвікові раси (Vampire/Skeleton/Ghost, `minAge==maxAge==0`) завжди Adult. `Actor::naturalDeathAge` — рахується один раз у конструкторі (`maxAge × 0.90..1.10`, той самий рандом-раз-і-назавжди підхід що вже є для інших речей), 0 = ніколи (безвікові). `ageStrMult()`/`ageSpeedMod()` — point-of-use модифікатори (той самий патерн що buff-тіки й hunger/thirst penalty, нічого не кешується в `strength`/`speed`); `ageStrMult()` вплетено прямо в `combat.h::resolveAttack()` (без змін у викликах — функція вже приймає `Actor&`). **Навички НЕ падають з віком** (docs: старий майстер лишається майстерним, просто фізично слабший — майбутні вчителі технік/магії). `tickAging()` (`main.cpp`) — раз на дію, раз на прожитий ігровий рік (`WorldTime::MINUTES_PER_YEAR` = 518400 — навмисно астрономічно довго, докс забороняє штучне прискорення) старить гравця/жителів/ворогів і вбиває тих, хто досяг `naturalDeathAge`, тим самим шаблоном повідомлення+лут що вже є для смерті від голоду/спраги. Заразом виявив і виправив прихований баг: O-панель і HUD рахували "ефективну швидкість" вручну (лише needs-penalty), тому бафи (Lightness/Acceleration/Slowness) впливали на реальний хід, але не відображались на екрані — тепер `Actor::effectiveSpeed()` (складає needs+вік+бафи в одному місці) використовується і в `tickWorld()`, і в обох UI-панелях, і заразом вороги вперше отримали `needsSpeedPenalty()` в `tickWorld()` (раніше — тільки гравець, асиметрія була недоглядом, не рішенням). Тестування: `setage <age>` в чіт-консолі (`cheat_console.h`) — без цього довелось би чекати 518400 дій на рік. Свідомо не зроблено: продуктивність підлітків/юнаків (немає механіки трудового виходу щоб зачепитись), вчителі-ветерани (немає системи вчителів), вплив на швидкість жителів (`Villager` рухається окремою системою, не energy/speed — той самий задокументований розрив що й у Lightness/Acceleration)
- [x] Цілі NPC → квести (`actor.h`, `npc.h`, `main.cpp`, roadmap з `village.md`) — перший квест у грі. `Actor::id` (новий, з `inline int nextActorId` лічильником у конструкторі) — стабільний ідентифікатор, що переживає `enemies.erase(remove_if(!alive))` (індекси у векторі зсуваються, id ні), потрібен щоб ціль квесту не "стрибала" на іншого ворога. `Villager::goalTargetEnemyId`/`goalAccepted` — ціль реальна (конкретний вже заспавнений `Enemy` за id, не текст-заглушка): `spawnVillagers()` наприкінці, якщо в секторі є вороги, обирає одного випадкового не-дитину жителя і призначає йому ціль. Завершення квесту НЕ трекається окремим прапорцем — рахується "на льоту" через `findEnemyById()` в `buildDialogueOptions()`: якщо ворога нема в `enemies` — він мертвий (у межах одного відвідування сектора інакше бути не може, бо зміна сектора однаково зносить і жителів, і ворогів разом). Діалог (`buildDialogueOptions()`) — новий пункт що змінюється за станом: ціль жива+не прийнято → "Ask about their trouble" (розкриває проблему, підменю "I'll deal with it"/"Not my problem"); ціль жива+прийнято → нагадування без зміни стану; ціль мертва+прийнято → "Return about their trouble" (нагорода 15 Gold Coin через `player.addToContainer()`, той самий виклик що й `TradePanel::buy()`). Нагорода лише якщо квест був прийнятий — сама собою вирішена загроза без діалогу нічого не дає. Компас-хінт (`compassHint()`, `main.cpp`) — грубий текст "42 tiles to the northeast" від гравця до цілі, показується при розкритті цілі й на нагадуванні "Any luck?"; тимчасова заглушка замість реального маркера/вейпоінта, якого ще нема. **Свідомо спрощено відносно `village.md` (задокументовано, не забуто):** (1) немає професії Мисливець (`Occupation` не має Hunter) — ціль універсальна, будь-який дорослий/підліток житель; (2) NPC сам НЕ йде битись із ціллю — `Villager::WANDER` обмежений 18 тайлами від ліжка, вороги не спавняться ближче 55 тайлів від центру села, зони фізично не перетинаються, а "хто піде сам" AI за межі села не існує; ціль реальна, але пасивна, доки гравець не втрутиться; (3) квест не переживає зміну сектора — жителі й вороги однаково перегенеровуються при вході в сектор (нема системи збереження), тому квест валідний лише в межах одного безперервного відвідування — не баг, чесна відповідність поточній архітектурі; (4) нема окремої панелі квестів — статус перевіряється переговором з жителем повторно, той самий "один рівень глибини" підхід що й решта діалогу
- [x] Двигун демографії села (`simulateVillageYear()`, `main.cpp`, roadmap з `village.md`/`world.md`) — робить демографію процесом, а не одноразовим знімком при заселенні. Тикає раз на прожитий ігровий рік для **поточного завантаженого села** (map-aware; далекі села тикають окремим map-free рушієм — див. "Персистентний worldgen-стор" нижче). Чотири проходи: дорослішання (дитина → дорослий по расовому `minAge`), спадкоємність професії (робота померлого переходить подружжю, інакше першій дорослій дитині, повторна спроба щороку доки хтось не підійде), шлюби (20%/рік серед неодружених/овдовілих дорослих, з виключенням братів/сестер і батьків/дітей), народження (15%/рік на пару в стадії саме `LifeStage::ADULT`). `tickAging()` розбито на `tickAgingOneYear()` (тіло без змін) + новий `tickYearlyEvents()`, що проходить у циклі **кожен** пройдений рік замість лише виявлення що хоч один минув — інакше стрибок одразу на кілька років тихо загубив би зайві. Заразом виправлено прихований баг: цикл очікування/відпочинку (Wait) взагалі не викликав тик старіння — тобто смерть від старості (і тепер демографія) не могла спрацювати через єдиний реальний спосіб пропускати роки в грі. `spawnChild()`/`giveOccupation()` винесені в спільні функції — і початкова генерація села, і новий річний тик використовують той самий код. Тестування: `skipyears <n>` у чіт-консолі (1-1000 років) — без цього довелось би реально чекати 518400 дій на рік. Свідомо не зроблено: нові будинки/фізичний ріст села (дорослі діти й молодята й далі діляться батьківським тайлом), Legends-стиль структурований лог подій у самій грі (події йдуть у звичайний `panel.addMessage()`, капований на 40 рядків), міжсільські шлюби (пул лише в межах одного села)
- [x] Headless worldgen-симулятор (`src/tools/worldgen_sim.cpp`, окремий `.exe`, roadmap з `world.md`) — "Legends mode" за зразком Dwarf Fortress: консольний інструмент що прогонить демографію села на N років **без** вікна гри, гравця чи карти, і друкує хроніку подій + фінальний склад родини в stdout. Компілюється й лінкується окремо від `greystone.exe` (команда збірки вище) — можливо тому що `Villager`/`Actor`/`Occupation`/`raceTraits` живуть у header-only файлах (`actor.h`, `item.h`, `npc.h`) без залежності від рантайму SDL2 (лише `SDL_Color`/`SDL_Point` як прості структури) — новий `.cpp` підключає ці заголовки, ніколи не викликає `SDL_Init` чи будь-яку іншу `SDL_*` функцію, і не лінкується з `render.cpp`/`map.cpp`. Свідомо **не** той самий код що ігровий `simulateVillageYear()` — той прив'язаний до карти (пошук вільного тайлу для дитини, зіставлення професії з реальною будівлею), тут карти нема; той самий набір правил, окрема реалізація (сама абстрактна половина винесена в `village_history.h` — див. нижче). Стартовий склад села (`seedVillage()`) наближає композицію яку зазвичай дає `placeVillage()` (`map.cpp`): 2 ферми (45% шанс що одна стане Herbalist, як і в реальному ролі), плюс Коваль/Староста/Лісник — в реальній грі ці три НЕ гарантовані (`placeRoleBuilding()` може не знайти місця після 30 спроб і просто пропустити будівлю, всупереч давнішому формулюванню "обов'язкові" в цьому файлі), але це деталь фізичного розміщення без карти тут не застосовна, тому абстрактна версія завжди сіє всі п'ять. Використання: `worldgen_sim.exe [years=100] [villages=1] [seed=random]`, вивід у stdout (перенаправити у файл — `> legends.txt`). Свідомо не зроблено: кілька сіл на реальній оверкарті, екран створення світу з сідом/слайдером років, сільські події (пожежа/рейд/неврожай/епідемія — `world.md` називає їх у прозі, але без жодних конкретних чисел/правил де-небудь у доках чи коді)
- [x] Могили предків (`src/entities/village_history.h`, `src/core/terrain.h`, `main.cpp::spawnVillagers()`, roadmap з `world.md`) — "Могили і сліди історії", і, за прямою вимогою користувача, **реальний результат симуляції, не текстовий флейвор**. Абстрактний map-free рушій `worldgen_sim.cpp` (`simulateOneYear()`, `seedVillage()`-стиль хелпери, `areRelated()`, `hasReliableTrade()`, `pickFirstName()` тощо) винесено в спільний header-only `src/entities/village_history.h` (`namespace VillageHistory` — щоб не конфліктувати з однойменними map-aware функціями/константами `main.cpp`, які лишаються окремо) — і `worldgen_sim.cpp`, і нова фіча підключають той самий код замість третьої копії (дублювання вже двічі призводило до багів, зокрема шлюбів між рідними). Для кожного домогосподарства `spawnVillagers()` тепер: (1) засновує пару молодих дорослих ~60 років тому (`HISTORY_YEARS` — спершу було 30, але за 30 років смерть від старості математично неможлива для засновників 18-26 років при мінімальному `naturalDeathAge`~72, тож могил не з'являлось взагалі; піднято до 60, де досяжний вік засновника (78-86) реально потрапляє в діапазон смерті), (2) прогонить `VillageHistory::simulateOneYear()` 60 разів **ізольовано для цього домогосподарства** (без міжсімейних шлюбів під час пре-історії — сьогоднішня модель ліжок цього не витримає), відстежуючи рік смерті кожного, (3) сьогоднішній мешканець ліжка — хто зараз тримає професію будівлі, або (якщо ремесло вимерло) найстарший живий нащадок; якщо взагалі ніхто не вижив (рідкісно) — ліжко просто лишається порожнім, а не вигаданий мешканець; (4) кожен хто помер отримує `O_GRAVE` (нове, `terrain.h` — символ `x`, свідомо не Unicode-гліф: доки вже документують що Miscellaneous Symbols не рендеряться цим шрифтом, а Block Elements — так, тож для нового об'єкта обрано безпечний ASCII; `durability=0` — той самий недоторканний сентінел що й криниця). **Один спільний цвинтар на село** (за проханням користувача, не могили розкидані за кожним будинком) — `villageCemeteryX/Y` (глобальні, `main.cpp`, поруч з `villageWellX/Y`), випадкове зміщення 15-28 тайлів від криниці; `placeGraveNear()` (вільна функція, ring-search до 8 тайлів, best-effort) природно кластерує повторні виклики на той самий анкер, бо кожна вже поставлена могила рахується як зайнята. `Grave` (`main.cpp`, поруч з `Corpse`) — permanent запис (без decay, на відміну від `Corpse`) з ім'ям/віком на момент смерті/роками тому відносно "зараз" (немає ще абсолютного календаря світу — окремий пункт "Екран створення світу"). Examine (right-click) показує "Here lies NAME. Died N years ago, aged AGE." — той самий патерн `hasCorpse`/`corpseName` що вже є в `ExaminePanel`, просто `hasGrave`/`graveName`/`graveAge`/`graveDiedYearsAgo`. Свідомо не зроблено: шлюби між домогосподарствами під час пре-історії, доросліші вцілілі родичі поза прямою лінією (сестра що не успадкувала ремесло тощо) просто не розміщуються на карті. (Повноцінний персистентний worldgen для ВСІХ сіл — зроблено, див. наступний пункт.)
- [x] Жива AI-поведінка поховання (`src/entities/corpse.h`, `src/entities/npc.h`, `main.cpp`, roadmap з `world.md`) — коли житель помирає ПІД ЧАС гри (не в пре-історії), інший житель тепер фізично несе тіло до цвинтаря замість того щоб труп просто розкладався сам (`CORPSE_DECAY_MINUTES`). `Corpse` отримав стабільний `id` (`nextCorpseId`, той самий патерн що `Actor::id`/`nextActorId`) і `age` (знятий з `Actor` у момент смерті) — раніше труп ідентифікувався лише позицією, а похоронному AI потрібно посилатись на "той самий труп" через багато тіків навіть якщо вектор `corpses` перевпорядкується (decay стирає елементи). Два нових стани `Villager::State` — `GO_TO_CORPSE`/`CARRY_TO_GRAVE` — перевикористовують ті самі поля шляху (`homePath`/`homePathIdx`/`pathRetryCool`/`lastDoorX/Y`), що вже й так спільні для `WALK_HOME`/`DRINK`/`EAT` (не окремий буфер на кожен стан), і той самий `followPath()`. `assignBurial()` (`main.cpp`, викликається в кінці `dropVillagerLoot()` — єдина точка через яку проходять усі смерті жителів, ворожі трупи (`dropEnemyLoot()`) не чіпає, свідомо лише жителі) обирає носія: спершу родич (чоловік/дружина → мати → батько → перша доступна дитина) якщо вільний (`alive && !isChild && state==WANDER`), інакше найближчий вільний житель за Chebyshev-відстанню; якщо взагалі нікого немає — труп просто розкладається як і раніше (той самий graceful-degradation прецедент що й у `placeRoleBuilding()`). `placeGraveNear()` витягнуто з локальної лямбди `spawnVillagers()` у вільну функцію — і пре-історія, і жива похоронна AI викликають той самий код на той самий `villageCemeteryX/Y`. Свідомо не зроблено (задокументовані обмеження, не забуті): немає resume після переривання — якщо носія силою перемикає на `FLEE`/`FIGHT` (`villagerReactToAttack()`, спрацьовує незалежно від поточного стану), завдання просто кидається; для `CARRY_TO_GRAVE` це означає тіло (вже знято з `corpses` при підйомі) зникає без могили — рідкісний збіг обставин, не вирішено; немає повторного сканування на нового вільного носія пізніше, якщо в момент смерті ніхто не був доступний (усі в бою/сплять) — труп просто розкладається, як і раніше

- [x] Персистентний worldgen-стор + подієва симуляція віддалених сіл (`src/entities/village_history.h`, `main.cpp`, roadmap з `world.md`, кроки 2 і 5) — раніше `spawnVillagers()` наново прогонив 60-річну пре-історію кожного домогосподарства **щоразу**, коли гравець заходив у сектор села, на несідованому `rand()`-потоці — тому склад сім'ї й могили мінялись щоразу при повторному вході, хоча `Grave` називалась "permanent". `generateAllVillageHistories()` (`main.cpp`, викликається один раз у `main()` одразу після `overmap.generate()`) тепер прогонить пре-історію **всіх** ~15 сіл оверкарти наперед, детерміновано (той самий seed-за-координатами підхід що й `generateSector()`, з іншою сіллю), і кешує результат у `villageHistoryStore` (`std::map<pair<secX,secY>, VillageHistoryRecord>`). Нові `VillageHistory::HouseholdHistory`/`VillageHistoryRecord`/`simulateHousehold()`/`simulateVillageHistory()` (`village_history.h`) — та сама механіка що раніше жила інлайн у `spawnVillagers()`, тепер фабрика: 2 фермерських + 3 ремісничих домогосподарства на село, професія під час симуляції — заглушка (`FARMER`/`WOODCUTTER`, конкретне значення enum не впливає на `hasReliableTrade()`/спадкоємність — важливий лише факт "є ремесло"), бо Фаза A не має карти і не може знати наперед, яку саме будівлю фізично згенерує `placeVillage()` (може пропустити Коваля/Старосту/Лісника після 30 невдалих спроб — задокументована поведінка) чи яка з двох ферм стане Herbalist (45%-рол, теж карта-залежний). `spawnVillagers()` тепер лише **читає** кеш (зіставлення по типу farm/trade, `nextFarmHousehold`/`nextTradeHousehold`), а реальну професію проставляє заднім числом резолвленому голові домогосподарства (`hist[headIdx].occupation = occ`) — пошук голови відтепер перевіряє і плейсхолдер, і вже проставлену реальну назву (інакше повторний візит губив ідентичність голови). **Подієва симуляція віддалених сіл** (крок 5): `tickYearlyEvents()` (раніше тикав лише поточне завантажене село через map-aware `simulateVillageYear()`) тепер додатково викликає `advanceDistantVillageHistories()` — той самий map-free рушій (`VillageHistory::advanceVillageOneYear()`/`advanceHousehold()`, обгортка над `simulateOneYear()`) тикає **кожне інше** закешоване село на один рік, тож повернувшись у село через багато років гравець бачить реальний результат (нові спадкоємці/шлюби/смерті), а не заморожений при генерації світу знімок. Могили: розстановка тепер трекає `HouseholdHistory::gravesPlacedThroughYear` (замість одноразового прапорця на все село) — при кожному візиті ставляться могили лише для тих, хто помер після попереднього чекпоінту (покриває і першу пре-історію, і смерті що сталися поки село тикало віддалено), без дублювання вже поставлених. Заразом виправив прихований баг фізичного шару: `placeGraveNear()` пише `O_GRAVE` прямо в тайл карти, а карта сектора **повністю перегенерується з нуля** (`generateSector()`) при кожному вході — без окремого кроку "перештампувати відомі могили на свіжу карту" (новий блок на початку `spawnVillagers()`, ітерує `graves` відфільтровані по поточному сектору) цвинтар фізично зникав при виході й поверненні, хоча записи `Grave` й лишались у пам'яті; стосувалось і пре-історичних, і щойно похованих під час гри (`assignBurial()`) могил, бо обидва типи лежать в тому самому `graves`-векторі. **Стрибки часу вже тикають демографію без додаткової роботи** (крок 6 `world.md`) — `tickYearlyEvents()` й раніше викликався і з `onPlayerAct()`, і з циклу очікування/сну (`WaitPanel`, раз на симульовану хвилину), і зі `skipyears`-чіту (одразу після стрибка, з внутрішнім `while` що доганяє будь-яку кількість пройдених років) — тепер, коли він тикає й віддалені села, усі три шляхи автоматично покривають і їх; окремого "подорожі" ("fast travel") ще нема (не заплановано нижче) — тільки сон/чіт. Свідомо не зроблено: поки гравець ФІЗИЧНО стоїть у селі, кеш цього села не синхронізується з живою `simulateVillageYear()`-симуляцією в реальному часі — підхоплює зміни лише при наступному вході після виходу (окрема, більша робота)

- [x] Legends Viewer (`src/entities/village_history.h`, `src/ui/cheat_console.h`, `main.cpp`, `src/tools/legends_viewer.cpp`, roadmap з `world.md`) — DF-style окрема програма для перегляду хроніки й складу кожного села, на прохання користувача. `LegendEvent`-лог кожного села раніше одразу викидався (`simulateHousehold()`/`advanceHousehold()` створювали локальний `std::vector<LegendEvent> log;` і ніде його не зберігали) — тепер обидві функції приймають `std::vector<LegendEvent>& log` ззовні, і `VillageHistoryRecord` отримав поле `log` (злитий по всіх 5 домогосподарствах, `std::stable_sort` за роком одразу після `simulateVillageHistory()` — бо домогосподарства симулюються послідовно одне за одним, не порічно вперемішку). Нова команда чіт-консолі `legends [filename]` (`pendingExportLegends`/`pendingLegendsFilename`, той самий pending-патерн що й `skipyears`/`give`) — обробник `exportLegends()` (`main.cpp`) пише простий pipe-delimited текстовий файл (без JSON-бібліотеки, у стилі решти проєкту) з хронікою + ростером кожного закешованого села з `villageHistoryStore`. **`src/tools/legends_viewer.cpp`** — новий третій standalone `.exe` (поруч з `worldgen_sim.exe`), і, на відміну від нього, **не підключає жодних заголовків гри й не лінкує SDL2 взагалі** — формат текстовий, парситься напряму (`<iostream>/<fstream>/<sstream>` тільки), тож команда збірки без жодного `-I`. Свідомо читає експорт з **реальної гри**, а не сам щось генерує (`worldgen_sim.exe` і далі генерує довільний світ на власному seed — окремий інструмент з іншим призначенням): інтерактивне консольне меню — список сіл з номерами → вибір → повна хроніка (рік: текст, за порядком) + склад домогосподарств (ім'я/вік/статус/професія) → Enter назад до списку; окрема команда `s` — підрядковий пошук по імені серед усіх сіл. **Доповнення (на прохання користувача):** села тепер мають рандомну фентезійну назву (`VillageHistory::generateVillageName()`, `village_history.h` — префікс+суфікс пул, напр. Alderbrook/Thornfield, у стилі решти англомовних генерованих імен гри; `VillageHistoryRecord::name`, присвоюється в `generateAllVillageHistories()`, той самий collision-avoidance патерн що й `pickFirstName()`) замість лише координат сектора — потрапляє в `VILLAGE`-рядок експорту, і `legends_viewer.exe` тепер дає обирати село номером **або** введенням (частини) назви прямо в головному меню, не лише зі списку. Свідомо не зроблено: фільтри за типом події чи роком, крос-village зв'язки, самі написи на могилах/розповіді NPC що читають цей лог (окремий пункт роадмапу) — зараз лог лише для перегляду людиною ззовні гри

- [x] Фізичний світ: висоти/річки/родючість/логічне розселення (`src/core/overmap.h`, `src/core/map.cpp`, roadmap з `world.md`, Шар 1-2) — `Overmap::generate()` більше не розкидає біоми випадковими плямами: `generateHeightfield()` — ручний "value noise" (грубий 10×10 рандом-грід → білінійна інтерполяція до 100×100 → box-blur), `classifyBiomes()` — температура (широта, symmetric від центру карти, з шумовим джиттером) + незалежна вологість-noise → існуючі 6 `BiomeType` через просту 3×3 таблицю (Cursed Lands лишився рідкісним аномальним оверлеєм зверху, не кліматичною зоною), `traceRivers()` — кілька витоків з найвищих секторів, трасування найкрутішим спуском до краю карти/злиття/локального мінімуму (озеро), `computeFertility()` — з висоти + відстані до води, `placeVillages()` — заміняє рандомний scatter на відсортований за родючістю вибір (той самий spacing-guard 5 секторів). Нові поля на `SectorInfo`: `height`/`isWater`/`flowDX,flowDY`/`fertility` (температура/вологість — лише транзитивні, не зберігаються). Водні сектори рендеряться окремим синім `=` (не тим самим `~`, що Swamp — щоб не плутати в легенді), інфобокс оверкарти показує висоту/родючість під курсором. `map.cpp::generateSector()` отримав нові параметри (`hasRiver, flowDX, flowDY`) і `carveRiver()` — фізично вирізає воду в секторі (озеро — пляма в центрі; річка — хвиляста смуга від краю проти течії до краю за течією, best-effort, не пікселево ідеальний стик із сусіднім сектором). Свідомо не зроблено (задокументовано як окремі наступні кроки): місто/замок (концепції "регіон" не існує), дороги через A* по оверкарті, профіль села від місця (доки самі кажуть "майбутнє"), нові типи `BiomeType` (гори/океан як окремий біом — замість цього висота/вода — bool/float поля поверх існуючих 6 типів, щоб не займатись `biomeTex[6]`)

- [x] Плавання (`src/entities/actor.h`, `src/core/terrain.h`, `src/core/map.h`, `main.cpp`, на прохання користувача, продовження фізичного світу вище) — вода була повністю непрохідною (`moveCost=0`, як Bedrock); тепер `moveCost=280` (повільніше за все інше — Swamp 200, Mud 170), тож `walkable()`/A* автоматично роблять її прохідною-але-дорогою без жодного спеціального case'у в pathfinding. Реальна небезпека: `Actor::spendStamina()` (готовий інтерфейс, досі нічим не використовувався) тепер витрачається на кожен вхід у водну клітинку (`SWIM_STAMINA_COST=8`, `moveActorTo()` — новий generic-over-Actor хелпер у `main.cpp`, яким тепер проходять **усі** 7 місць руху акторів: гравець (`updatePlayer()`), ворог (flee+chase), житель (flee+combat-chase+`followPath()`+`WANDER`) — симетрія, не лише гравець). `Actor::tickNeeds(bool inWater)` (новий параметр, дефолт `false` — старі виклики без аргументу й далі компілюються) — поки стоїш у воді, регенерація стаміни вимкнена (тільки на суші), а на нулі стаміни — утоплення: `torso.hp -= 5` щотик, той самий "torso — ворота смерті" патерн що й кровотеча. Свідомо **поступове** утоплення, не миттєва смерть в момент коли лічильник впав до нуля (user-запит: миттєва смерть без вікна на реакцію відчувалась би нечесно). Заразом виправив побічний баг, який ця робота оголила: `Tile::blocksVision()` прирівнювала "непрохідне" (`moveCost==0`) до "блокує зір" — коректно для Bedrock (суцільна скеля), але вода випадково успадкувала блокування зору звідти ж, хоча крізь воду має бути видно; `TerrainDef` отримав окреме поле `blocksVision` (той самий патерн, що вже є в `ObjectDef` — низький камінь блокує рух, не зір), виставлене `true` лише для Bedrock. Гравець отримує повідомлення (throttled, той самий патерн що голод/спрага) коли починає тонути. Свідомо не зроблено: індикатор у Панелі ефектів (стаміна й так завжди видна в HUD як "STA"), вплив ваги екіпіровки/інвентаря на витрату (наступний крок, якщо знадобиться), спеціальна обробка "ворог не повинен тонути переслідуючи гравця через річку" (наразі це реальний, симетричний ризик для будь-кого хто зайде у воду)
- [x] Кровотеча: природне загоєння з часом (`actor.h`, на прохання користувача) — раніше `bleed` ніколи не спадав сам, тільки Herbal Poultice скидав його миттєво, що на практиці означало "кожна кровотеча вічна без предмета". Новий `Body::clot()` (виклик з `tickNeeds()`, той самий каданс що голод/спрага/стаміна-реген) зменшує `bleed` кожної з 6 частин на 0.01/тик — але **лише легку** кровотечу (`bleed<=1.0`, WOUNDED-поріг чи форс від меча): важка (`2.0`, SEVERE-поріг чи `Brutal Strike`) не спадає сама, потребує реального лікування (user-запит: "легкий поріз сам проходить, сильний — тільки лікувати"). Легка кровотеча спиняється сама приблизно за 100 ігрових хвилин (~1.7 год)
- [x] Дороги через A* по оверкарті (`src/core/overmap.h`, `src/core/terrain.h`, `src/core/map.cpp/h`, `main.cpp`, останній пункт Шару 2 з `world.md`) — `findOvermapPath()`, нова окрема реалізація A* з octile-евристикою над 100×100 `sectors[][]` (той самий алгоритм що `src/core/astar.cpp::findPath()`, але не той самий код — `astar.cpp` жорстко прив'язаний до 200×200 тайлової сітки сектора й `Tile::walkable()/moveCost()`, зовсім інший тип даних). Вартість кроку — `100 + height×300` (рівнина дешева, гори дорогі), `800` якщо `isWater` (дорого, не заборонено — річку можна перебрести, мостів нема). **`buildRoads()`** (новий крок у `Overmap::generate()`, після `placeVillages()`) — MST (Prim's, O(n²)) з'єднує всі ~15 сіл за евклідовою відстанню (дешевий proxy замість ганяння A* по всіх C(15,2)=105 парах — лише для ~14 обраних MST-ребер), для кожного ребра реальний `findOvermapPath()` дає фізичний маршрут; кожен пройдений сектор — нові поля `SectorInfo::hasRoad`/`roadDX`/`roadDY` (той самий патерн що `isWater`/`flowDX`/`flowDY` для річок). Рендер оверкарти — окремий символ `+` (пропускається на секторах з селом — маркер `#` і так домінує), новий рядок легенди. Фізичне вирізання — новий **`carveRoad()`** (`map.cpp`, той самий "крок від довжини шляху" підхід що й `carveRiver()`, викликається останнім у `generateSector()`, після `placeVillage()`) малює новий ground cover **`G_ROAD`** (`terrain.h`, перший **від'ємний** `moveCostMod=-20` у таблиці — дорога реально трохи швидша за траву, не лише косметика) замість заміни terrain (трава/пісок під дорогою лишається візуально); спершу просто пропускала будь-який зайнятий тайл (щоб не знести стіну чи криницю), але це лишало дерева/каміння прямо на дорозі (user report) — тепер `isClearableWildObject()` явний allowlist (`O_TREE/O_DEAD_TREE/O_BUSH/O_ROCK/O_BOULDER/O_FALLEN_LOG/O_HERB/O_MUSHROOM` — точно те, що `spawnObject()` розкидає по дикій місцевості) прибирає лише "дике", а все збудоване чи вирощене (стіни, двері, комора, **криниця**, пшениця, надгробок, пень) як і раніше обходить, не займаючи — той самий підхід, що вже є в `pathToPlaza()` для внутрішньосільських стежок, просто вибірковий, а не "все чи нічого". Для сектора села кінцева точка — центр карти (той самий якір що `placeVillage()`/`villageWellX/Y`), не протилежний край. Свідомо не зроблено: мости (річка просто дорожча для A*-планування, фізично той самий брід що й плавання), вузли з кількома дорогами (`SectorInfo` зберігає один напрям на сектор, як і річки — останнє MST-ребро виграє), міста/замки (не існують у коді)
- [x] Річка/дорога тепер стикуються між секторами (`overmap.h`, `map.cpp`, user report: "інколи криво ставляться... коли з одного тайла в інший тайл переходиш") — раніше `carveRiver()`/`carveRoad()` незалежно вигадували свою точку входу/виходу на межі сектора (джиттер відносно центру), тож сусідні сектори майже ніколи не сходились рівно. Новий **`crossingCoord()`** (`map.cpp`) — детермінований order-independent хеш пари координат двох сусідніх секторів → точка перетину межі; обидва сектори викликають його з тим самим (незалежно від порядку) — тож завжди узгоджені. `SectorInfo` отримав `riverFromDX/riverFromDY` і `roadFromDX/roadFromDY` (той самий патерн, що вже `flowDX/DY`/`roadDX/DY` — тепер напрям "звідки" зберігається окремо від "куди", виставляється в `traceRivers()`/`buildRoads()` при кожному кроці). Новий **`edgePoint()`** (`map.cpp`) — точка на краю сектора для конкретного сусіда (пряма — хеш; діагональна — кут, без хешу, обидві сторони й так рахують той самий кут); `carveRiver()`/`carveRoad()` тепер починають/закінчують саме там, а не в джиттерованій точці. Заразом це виправило приховану ваду навіть **всередині** одного сектора: якщо річка входить з півночі, а виходить на схід (згин), стара логіка все одно вважала що вхід — з протилежного до виходу боку (тобто з півдня) — тепер вхід і вихід рахуються незалежно, кожен зі свого напряму
- [x] Врожай тепер реально відростає (`main.cpp`, перший крок "Економічні потоки" з `village.md`, на прохання користувача) — виявлений прихований баг: `calcPlantAge()` (`map.cpp`) — детермінована формула з дати+координат — рахувалась **лише** при генерації сектора, даючи свіжому полю розкидану стиглість; після цього нічого й ніколи не рухало `plantAge` вперед знову — і зібраний врожай (`plantAge=0` після жнив), і навіть НЕ зібраний, але ще незрілий на момент генерації тайл лишались назавжди в тому самому стані без чіт-команди `grow_plants`. Новий **`tickPlantGrowth()`** — той самий "static lastX, while пройшло" каданс що й `tickYearlyEvents()`, раз на прожитий ігровий день, для поточного завантаженого сектора (той самий обсяг, що й решта живих per-sector тіків — вогонь, потреби) — піднімає `plantAge` кожного рослинного об'єкта на `255/daysToMature` за день, той самий темп що й `calcPlantAge()` використовує при генерації, тож ритм регенерації узгоджений. Пауза (не відкат) поза `growSeasons` — озима пшениця чекає, не в'яне. Підключено в тих самих 3 місцях, що й `tickYearlyEvents()` (`onPlayerAct()`, `skipyears`-чіт, цикл очікування) — і чіт `skipyears`/тривале очікування тепер реально повертають гравця до дозрілого поля. Працює для будь-якого рослинного об'єкта (`isPlant`), не лише сільськогосподарського — дикі гриби/трави в лісі теж тепер відростають. Свідомо не зроблено (лишається для наступних частин цього ж пункту роадмапу): фізичне сіяння (сама дія посадки — зараз рослина завжди "вже там", просто росте), фермер що фізично несе врожай (досі миттєве сканування-споживання при голоді, не хода на поле й назад)
- [x] Торгівля між NPC (`entities/npc.h`, `main.cpp`, останній шматок "Економічні потоки" з `village.md`, на прохання користувача — "коваль купує хліб у фермера") — раніше `EAT`-стан жителя мав лише 3 джерела їжі (своя сумка → сімейна комора → жнива, останнє тільки для Farmer/Herbalist) — не-фермер (коваль, лісоруб, староста) з порожньою сумкою й без комори просто голодував назавжди без жодного виходу, коли стартовий запас `goodsFor()` вичерпувався. Новий `Villager::State::BUY_FOOD` (+ поле `tradeTargetId`) — коли `EAT` не знаходить їжі і житель не Farmer/Herbalist, `findGranarySeller()` (Chebyshev-відстань від ліжка покупця, той самий метод що вже кластерує домогосподарства в `spawnVillagers()`) шукає найближчого **іншого** жителя з коморою; якщо є золото — житель фізично йде до чужої комори (`followPath()`, той самий генерик-буфер що й WALK_HOME/EAT/DRINK), і на місці платить продавцю НАПРЯМУ в сумку (не в комору — монети не їжа, той самий патерн що вже в `TradePanel` для гравця), забирає одну порцію їжі, одразу з'їдає. Ціна — `Item::value` (той самий "приблизна вартість у золоті", що вже існує для гравцевої торгівлі, нічого нового не додавав). Якщо продавця чи золота нема — голод лишається високим, як і раніше (реальний ризик, не просто фантик). Симетрично для будь-якого не-фермера, не лише коваля — все узгоджено з тим, як `village.md` описує замкнутий цикл
- [x] Фізичний цикл жати/нести (`entities/npc.h`, `main.cpp`, довершує "Економічні потоки" з `village.md`, на прохання користувача) — жнива фермера були миттєвим сканування-споживанням: зголоднів → код за один тік знаходить стиглий тайл у радіусі 6 навколо ліжка, обнуляє `plantAge`, кладе хліб просто в комору. Тепер два нових стани — **`HARVEST`** (`entities/npc.h`) шукає конкретний стиглий тайл (той самий пошук, що й раніше, лише не виконує жнива одразу — записує координати в нове поле `harvestTargetX/Y`), фізично йде туди (`followPath()`), і тільки прийшовши збирає врожай **у свою сумку** (не в комору напряму); **`CARRY_HARVEST`** — йде від поля до комори (`granaryX/Y`) і там перекладає весь запас їжі з сумки в комору ("несе снопи в комору", не тільки на один прийом їжі — комора відтепер спільний сімейний запас, не особиста сумка фермера), після чого повертається в `EAT`, де вже існуюча логіка (їсть з комори) природно замикає цикл. Захист від "тайл вже не стиглий, поки йшов" (регрост тепер поступовий, не миттєвий) — просто здається і повертається в `WANDER`, той самий tolerance що й решта станів. Свідомо не зроблено: власне сіяння як окрема дія (`docs` описують "сіє → росте → жне", але оскільки `tickPlantGrowth()` вже примушує будь-яку рослину — зібрану чи ні — рости сама по собі, окрема команда "посадити" не додає нічого механічно нового поверх уже існуючого регросту, і не додав штучно заради галочки)
- [x] Живі події поточного села тепер пишуться в Legends-журнал (`main.cpp`, на прохання користувача — "програма має записувати лог кожного села кожного жителя, щоб... розібратись хто вмер хто женився") — раніше `VillageHistoryRecord::log` (той самий, що читають чіт `legends`/`legends_viewer.exe`) наповнювався лише пре-історією й тіканням **віддалених** сіл (`advanceDistantVillageHistories()`); усе, що ставалось живцем, поки гравець фізично стояв у **поточному** селі, йшло тільки в скороминущий `panel.addMessage()` — зникало з екрана назавжди. Новий `logVillageEvent()` (поруч з іншими глобалами, до будь-якого виклику) пише в `villageHistoryStore[{playerSectorX,playerSectorY}].log` з абсолютним ігровим роком від старту кампанії (`worldTime.minutes/MINUTES_PER_YEAR` — навмисно **не** той самий рахунок, що `yearsSimulated` цього села, який заморожений, поки село живе, той самий уже задокументований розрив). Підключено: усі 4 події `simulateVillageYear()` (дорослішання/спадкоємність/шлюб/народження), і, найважливіше — **одним хуком** у `dropVillagerLoot()` (єдина точка, через яку проходить смерть жителя **незалежно від причини** — старість/голод/бій/вогонь/утоплення/розчавлення, 11 місць виклику з різними причинами) замість полювання за кожним окремим "X dies"-повідомленням; плюс нові з цієї ж сесії — торгівля NPC (`BUY_FOOD`) і завершення жнив (`CARRY_HARVEST`, лише в журнал, без спаму в чат — часта подія). Причина смерті в журналі не зберігається (лише хто/коли) — залишається тільки в скороминущому чаті, як і раніше
- [x] Абстрактна економіка для **не завантажених** сіл + чіт `simulatedays` (`entities/village_history.h`, `src/ui/cheat_console.h`, `main.cpp`, на прохання користувача — "легенда не має залежати від гравця") — реальна торгівля/жнива (`BUY_FOOD`/`HARVEST`/`CARRY_HARVEST`) працюють лише для села, де гравець фізично стоїть (потребує тайлів/шляхів). Для решти сіл — новий **`logEconomicFlavor()`** у спільному map-free рушії (`village_history.h`, той самий, що вже генерує пре-історію й тикає `advanceDistantVillageHistories()`) — раз на рік, ~20% шанс на домогосподарство, суто наративний рядок у журнал ("X's household brings in a good harvest" для фермерських, "X trades with the farms to keep the household fed" для реміснических) — без жодних реальних Item, той самий рівень абстракції, що вже й так є для шлюбів/народжень/смертей у пре-історії. Працює автоматично для **кожного** села, включно з тими, куди гравець ще не заходив — саме через це воно вже не залежить від гравця. Окремо — **`simulatedays <n>`** (той самий pending-патерн що `skipyears`, 1-3650 днів) — на відміну від `skipyears`, який лише перестрибує календар, ця команда реально прокручує кожну симульовану хвилину (той самий цикл, що й `Wait`-панель — `worldTime.advance()`, `tickWorld()`, `player.tickNeeds()`, `tickVillagerNeeds()`, `tickYearlyEvents()`, `tickPlantGrowth()`), тож голод/спрага РЕАЛЬНО накопичуються і `BUY_FOOD`/`HARVEST` для **поточного** села тепер теж тестуються без ручного багаторазового натискання `T`

## Заплановано

Нижче — короткий знімок для орієнтиру, згрупований по тому самому GDD-файлу з якого він
походить. Це НЕ повний перелік — перед плануванням звіряйся з самим `docs/*.md` (див.
попередню секцію). Пункти позначені `~~закреслено~~` частково зроблені — деталі в
"Поточний стан" вище.

### Село (roadmap з village.md)

- [ ] **Worldgen сімей**: ~~будинки → сім'ї~~ базові зв'язки (подружжя/батьки/діти) зроблено вище; лишилось — брати/сестри між домогосподарствами, вдівці, сліди історії (могили, спадок)
- [ ] **Комора**: ~~контейнер-об'єкт у будинку, спільний на сім'ю~~ зроблено вище; лишилось — гравець не може її відкрити як контейнер, і сама фізична передача (фермер несе врожай, а не він телепортується в комору) досі спрощена
- [x] ~~Економічні цикли предметами~~: ~~врожай реально відростає~~ ~~передача Item NPC↔NPC~~ ~~фермер фізично жне і носить врожай~~ зроблено вище; свідомо не зроблено — окрема дія "сіяти" (нічого не додає механічно поверх уже існуючого автоматичного регросту)
- [ ] **Вік і стадії**: ~~класифікація стадій, вплив на speed/strength, смерть від старості~~ зроблено вище; лишилось — продуктивність підлітків/юнаків, вчителі-ветерани (Elder/Old), вплив віку на швидкість жителів (окрема система руху)
- [ ] **Демографія**: ~~шлюби, народження, ріст дітей, учнівство/спадкоємність професії~~ ~~тикання далеких сіл~~ зроблено вище (`simulateVillageYear()` для поточного села + `advanceDistantVillageHistories()` для решти); лишилось — шлюби між селами, нові будинки (село фізично росте)
- [ ] **Спадщина NPC**: ~~комора переходить спадкоємцю при смерті власника~~ зроблено вище (`transferGranary()`); лишилось — те саме для будинку/землі/іншого майна (потребує системи власності на землю, див. нижче)
- [ ] **Цілі NPC → квести**: ~~перший квест (жива ціль-ворог, діалог, нагорода)~~ зроблено вище; лишилось — професія Мисливець, NPC сам переслідує ціль, квест переживає зміну сектора, кілька цілей на жителя, панель квестів
- [ ] **Земля**: `owner` на ділянках, купівля/продаж у старости
- [ ] **Шлях до старости**: репутація + багатство + підтримка жителів

### Worldgen (roadmap з world.md)

- [x] **Шар 1 (фізичний світ)**: висоти (noise), річки за градієнтом, родючість землі — зроблено, див. "Поточний стан" нижче ("Фізичний світ: висоти/річки/родючість/логічне розселення")
- [x] **Шар 2 (розселення)**: ~~села за логікою місця (вода, родючість)~~ ~~дороги через A* по оверкарті~~ зроблено, див. "Поточний стан"; лишилось — міста/замки (багатосекторні поселення, концепції "регіон" ще не існує)
- [x] **Шар 3 (`simulateVillageYear()`)**: серце системи — один рушій, що старить/одружує/народжує/ховає жителів за правилами village.md. Задуманий для трьох застосувань одразу — усі три тепер працюють: worldgen (`generateAllVillageHistories()`, прогнати N років для кожного села наперед), подієва симуляція далеких сіл під час гри (`advanceDistantVillageHistories()`), прискорена симуляція під час стрибків часу (сон через `WaitPanel`/`skipyears`-чіт — уже тикали й раніше, тепер покривають і далекі села теж). "Подорожі" (fast-travel) як окремий тип стрибка часу ще не існує в грі — див. пункт нижче. Правила реалізовані двічі: `main.cpp::simulateVillageYear()` (map-aware, поточне завантажене село) і спільний map-free `src/entities/village_history.h` (`namespace VillageHistory` — використовують і headless `worldgen_sim.cpp`, і персистентний стор) — див. "Поточний стан" вище
- [ ] Екран створення світу (seed + слайдер років історії 5/50/150+ + живий лог подій) — стор під нього вже є (`generateAllVillageHistories()`), лишається лише UI/UX
- [ ] Могили і сліди історії в генерації сектора: ~~могили предків з реальної пре-історії домогосподарства~~ ~~жива AI-поведінка поховання під час гри~~ зроблено вище; лишилось — спадкові будинки вимерлих родів (порожнє ліжко зараз просто порожнє, без "цей дім колись належав роду Х")
- [ ] Legends-лог подій (`{рік, село, тип, учасники}`): ~~зберігання логу + перегляд окремою програмою~~ зроблено (`VillageHistoryRecord::log`, `legends` чіт, `legends_viewer.exe`) — див. "Поточний стан"; лишилось — самі написи на могилах/розповіді NPC, які читають цей лог (зараз лог тільки для перегляду людиною ззовні гри)
- [ ] Етапи 2-4 (з `world.md`): міжсільські зв'язки → фракції/війни/руїни → іменні легенди — свідомо далеко, не починати без Етапу 1

### Системи персонажа (roadmap з weapons.md / magic.md)

- [ ] **Оглушення булавою**: потрібна механіка "пропуск ходу" (stun/turn-skip) — не існує для жодного actor'а, включно з гравцем; ігнор 50% броні вже зроблено
- [ ] **Швидкість дії від зброї**: кинджал має бути дешевшим по часу — зараз кожна дія (рух/удар) коштує фіксовані 100 енергії, немає per-weapon variance
- [ ] **Спис**: атака за 2 тайли (тримає дистанцію) — потребує зміни range-перевірки атаки
- [ ] Ефекти кулаків (якають силу) і щита (блок+поштовх) — ще не має механіки блоку взагалі
- [ ] **Техніки — розширення**: вчитель-ветеран у селі (зараз техніки відкриваються автоматично за рівнем навички, без окремого "вивчено від когось"), Обезголовлення/Оглушливий удар/Поштовх щитом з таблиці weapons.md, chase-to-technique (зараз тільки впритул), Backstab проти ворогів (потребує системи "чи ворог мене помітив" — є лише для сплячого жителя)
- [ ] **Магія — решта школи Вогню й інші школи**: Тепло (потребує систему температури/холоду — її взагалі нема), Вогняний щит (реактивний тригер на вхідний удар), Метеор (відкладена/телеграфована дія), імунітет до вогню (потребує типізації урону — `takeDamage()` не розрізняє physical/fire), потім Вода/Земля/Повітря/Життя/Смерть. Каст зараз атомарний (як техніки) — багатоходовий переривний каст ("рух скасовує каст" з `magic.md`) ще не зроблено, поки що не критично при castTime 1-2 ходи

### Інше

- [ ] Система фракцій (`factionId` на Actor і досі не використовується; `disposition` тепер реально керує бойовою поведінкою жителів)
- [ ] Збереження / завантаження
- [ ] Повноцінні діалоги з гілками — базовий режим є (привітання + Trade/Ask/Farewell), справжнього дерева з авторським контентом ще нема
- [ ] Фаст-тревел по оверкарті
- [ ] Худоба/тваринництво (Rancher), м'ясник, винороб, тесляр — потребують системи тварин/полів за типом, поки що немає
- [ ] Ремонт предметів у коваля (зараз коваль лише продає)
- [x] ~~Торгівля між NPC (щоб не-фермери могли поповнювати їжу)~~ зроблено вище

---

## Файли

Код лежить у `src/`, розбитий на 4 підпапки за роллю. Заголовки лишаються
з пласкими `#include "file.h"` — компілятор шукає їх через `-I` на кожну
підпапку (див. команду збірки вище), а не через відносний шлях.

Дизайн-документи лежать у `docs/`: `magic.md`, `weapons.md`, `village.md`, `world.md`.

| Файл | Призначення |
|---|---|
| `src/main.cpp` | Головний цикл, input, AI, переходи секторів |
| `src/core/astar.cpp/h` | A* пошук шляху, константи карти |
| `src/core/map.cpp/h` | Tile, generateSector, updateVisibility |
| `src/core/terrain.h` | TerrainDef / GroundDef / ObjectDef + BiomeType |
| `src/core/time_system.h` | WorldTime — хвилини/години/дні/сезони |
| `src/core/overmap.h` | Оверкарта 100×100 |
| `src/render/render.cpp/h` | Ініціалізація текстур, рендер карти/акторів |
| `src/entities/actor.h` | Actor / Player / Enemy / Animal + Race + Body |
| `src/entities/skill.h` | `Skill` enum (зброя+магія) + `SkillLevel` — прогресія, спільна для Actor |
| `src/entities/combat.h` | `resolveAttack()` — формули попадання/урону, `randomHitPart()`/`partName()` |
| `src/entities/magic.h` | `Spell`/`SpellId`/`SpellShape`, `rollSpellCast()`/`resolveSpellHit()` — спільний "двигун" заклять, backfire на низькій навичці. Сам список заклять — у `spells_<school>.h` |
| `src/entities/spells_fire.h` | Заклять Вогню (`inline constexpr Spell`, фрагмент — підключається лише з `magic.h`) |
| `src/entities/spells_water.h` | Заклять Води (той самий фрагмент-патерн) |
| `src/entities/spells_earth.h` | Заклять Землі (той самий фрагмент-патерн) |
| `src/entities/spells_air.h` | Заклять Повітря (той самий фрагмент-патерн) |
| `src/entities/spells_life.h` | Заклять Життя (той самий фрагмент-патерн) |
| `src/entities/spells_death.h` | Заклять Смерті (той самий фрагмент-патерн) |
| `src/entities/item.h` | Item, EquipSlot, ItemType, фабрики предметів |
| `src/entities/npc.h` | `Villager : Actor` — маршрути, домівка, `Occupation`/`bag` |
| `src/entities/corpse.h` | Труп актора після смерті |
| `src/entities/enemy_types.h` | Фабрики типів ворогів |
| `src/entities/village_history.h` | Спільний map-free рушій демографії (`VillageHistory` namespace) — використовують `worldgen_sim.cpp`, і персистентний worldgen-стор (`VillageHistoryRecord`, `main.cpp::villageHistoryStore`), який читає `spawnVillagers()` |
| `src/tools/worldgen_sim.cpp` | Headless "Legends mode" — окремий `.exe`, прогонить демографію села на N років без карти/вікна гри |
| `src/tools/legends_viewer.cpp` | Legends Viewer — окремий `.exe`, читає текстовий експорт з реальної гри (чіт `legends`) і дає інтерактивно погортати хроніку/склад кожного села |
| `src/ui/inventory.h` | InventoryPanel (UI інвентаря) |
| `src/ui/item_examine_panel.h` | Панель перегляду предмета |
| `src/ui/examine_panel.h` | Перегляд тайла (+ предмета на ньому) |
| `src/ui/cheat_console.h` | Чіт-консоль (backtick) |
| `src/ui/context_menu.h` | Контекстне меню з lambda-діями |
| `src/ui/panel_style.h` | Спільний стиль попапів: `PanelStyle::frame()`/`dimBackdrop()` |
| `src/ui/menu_hub.h` | `MenuHub` — вкладки Character/Body/Effects/Techniques/Magic/Inventory/Craft/Map |
| `src/ui/ui.h` | Панель статів (O) |
| `src/ui/bottom_panel.h` | Лог повідомлень + HUD (HP/SPD/NRG/час, рядок статус-ефектів) |
| `src/ui/body_panel.h` | Стан частин тіла + голод/спрага (B) |
| `src/ui/effects_panel.h` | Панель активних бафів/дебафів (S) |
| `src/ui/skills_panel.h` | Панель навичок — зброя + магічні школи, рівень/прогрес (K) |
| `src/ui/techniques_panel.h` | Панель технік — вимоги/вартість, призначення на слоти хотбара (Y) |
| `src/ui/spells_panel.h` | Панель заклять — вимоги/вартість/ризик невдачі, призначення на слоти хотбара (P) |
| `src/ui/hotbar.h` | Хотбар 1-9 внизу вьюпорту — виклик техніки чи закляття клавішею/кліком |
| `src/ui/wait_panel.h` | Вибір тривалості очікування (T) |
| `src/ui/confirm_panel.h` | Модальний Так/Ні попап (переривання очікування дебафом) |
| `src/ui/craft_panel.h` | Крафт: рецепти, інгредієнти, `countItems`/`consumeItems` |
| `src/ui/trade_panel.h` | Торгівля з NPC-торговцем: купівля/продаж |
| `src/ui/villager_examine_panel.h` | Перегляд інформації про NPC |
| `src/ui/enemy_examine_panel.h` | Перегляд інформації про ворога |
| `src/ui/pickup_panel.h` | Панель підбору предметів з землі |